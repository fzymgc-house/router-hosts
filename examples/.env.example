# Router-hosts Docker environment configuration
# Copy to .env and customize for your system

# =============================================================================
# Docker User/Group IDs
# =============================================================================
# Set these to match your host user for proper volume permissions
# Find your values with: id -u && id -g

# Linux (most distros): 1000:1000
# macOS: typically 501:20
DOCKER_UID=1000
DOCKER_GID=1000

# =============================================================================
# Vault Configuration (for vault-agent setup)
# =============================================================================
# Vault server address (required for Vault Agent)
VAULT_ADDR=https://vault.example.com:8200

# Optional: PKI secrets engine path (default: pki)
# VAULT_PKI_PATH=pki

# =============================================================================
# Logging
# =============================================================================
# Router-hosts log level: trace, debug, info, warn, error
RUST_LOG=info
