---
milestone: v0.13.0
audited: 2026-08-02T02:05:00Z
status: tech_debt
scores:
  requirements: 8/8
  phases: 1/1
  integration: complete
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
nyquist:
  compliant_phases: []
  partial_phases: []
  not_validated_phases: ["01"]
  missing_phases: []
  overall: not_validated
tech_debt:
  - phase: 01-consumer-rendered-output-templates-sink
    items:
      - "UAT test 42 (resolver reload + two-node convergence) skipped — needs a real unbound host and a second machine"
      - "Four manual deployment verifications from plan 01-08 recorded NOT-RUN"
      - "None of the three e2e tiers run in ci-go.yml (#403) — proc_e2e is the only tier that catches CLI-flag regressions like G-01-1"
      - "store.ListAll still folds full event history server-side (#400, #401) — storage-layer half of lazy streaming deferred"
      - "docs/reference/cli.md hand-maintained because task docs:build output fails rumdl (#402)"
      - "01-VALIDATION.md still status: draft — validate-phase never reconciled it, so nyquist_compliant is not authoritative"
  - phase: cross-cutting
    items:
      - "service.go (1033 LOC) and commands.go (519 LOC) are merge hotspots"
      - "in-tree legacy_migration.go is a permanent maintenance surface pending a removal milestone"
      - "/gsd-ship's ship-note [ci skip] marker blocks required checks on this repo — see STATE.md"
---

# Milestone v0.13.0 — Consumer-Owned Output: Audit

**Status:** `tech_debt` — all requirements satisfied and no blockers, but accumulated
deferred items warrant review before the next milestone.

**Scope:** single phase — `01-consumer-rendered-output-templates-sink` (11 plans).

---

## Requirements Coverage (3-source cross-reference)

Each requirement was cross-checked against the REQUIREMENTS.md traceability table,
the phase VERIFICATION.md requirements table, and the `requirements_completed`
frontmatter of every SUMMARY.md.

| Req | Traceability | VERIFICATION.md | SUMMARY frontmatter | Final |
|-----|--------------|-----------------|---------------------|-------|
| TMPL-01 | `[x]` Complete | passed | 01-01, 01-08 | satisfied |
| TMPL-02 | `[x]` Complete | passed | 01-01, 01-02, 01-08 | satisfied |
| TMPL-03 | `[x]` Complete | passed | 01-01, 01-02, 01-07, 01-08 | satisfied |
| TMPL-04 | `[x]` Complete | passed | 01-01, 01-08 | satisfied |
| TMPL-05 | `[x]` Complete | passed | 01-05, 01-07, 01-08, 01-10, 01-11 | satisfied |
| TMPL-06 | `[x]` Complete | passed | 01-04, 01-06, 01-08 | satisfied |
| TMPL-07 | `[x]` Complete | passed | 01-03, 01-08 | satisfied |
| TMPL-08 | `[x]` Complete | passed | 01-01, 01-02, 01-05, 01-06, 01-07, 01-09 | satisfied |

**Orphan detection:** none. Every REQ-ID in the traceability table appears in the
phase VERIFICATION.md and in at least one SUMMARY frontmatter.

**Note on TMPL-05:** plan 01-05's SUMMARY explicitly flags its own listing as
partial (primitives only, no `WatchHosts` wiring) and did not flip the requirement.
Completion comes from 01-07 (client sink CLI) with 01-10/01-11 closing gap G-01-1.
The three-source rule still resolves this to **satisfied** — the partial claim is
superseded by later plans, not left dangling.

---

## Cross-Phase Integration

A single-phase milestone has no sibling phases, so the integration check was
directed at this phase's own subsystem seams and at its joins to already-shipped
surfaces (event store, gRPC server, existing output generators).

| Seam | Verdict |
|------|---------|
| WatchHosts RPC to render/watch CLI | wired — `render.go:59`, `watch.go:198` |
| Contract version gate | wired — server advertises at `watch.go:132`; client gates via `RequireVersion` before render |
| Atomic write | wired — `render.go`, `watch.go`, `sinkstatus.go` all route through `atomicfile.Write` |
| Change ID | wired — `LatestEventID` read before `ListAll` (`watch.go:89-97`), carried on `SnapshotComplete` |
| Sink health to OTel | wired — all seven gauges have observation points; none registered without being observed |
| Shared sanitizer | wired — one `internal/sanitize.CommentField` used by both server generators and the template FuncMap |
| `--config` plumbing (G-01-1) | wired — `Flags.Config` to `ClientConfigOverrides.ConfigPath` to LoadClientConfig layer 1 |

**Orphaned exports:** none found. No symbol produced by this phase is left unconsumed.

---

## End-to-End Flows

| Flow | Verdict |
|------|---------|
| One-shot render | complete |
| Sink watch (follow, sidecar status, post-write hook) | complete |
| Reconnect after server drop, no truncated artifact | complete |
| Change-ID convergence | complete |
| Sink health keyed by mTLS CN | complete |

---

## Evidence Run at Audit Time

These were executed during this audit, not carried over from phase artifacts:

| Check | Result |
|-------|--------|
| `task test` (race detector) | all packages ok |
| `task test:e2e` (in-process) | PASS, incl. `TestE2E_WatchSinkSurvivesServerRestart` |
| `task test:e2e:proc` (real processes) | PASS, incl. `TestProcE2E_ColdStartWatchHonorsConfigFlag` |
| `task test:coverage:ci` | 86.3% (threshold 80%) |
| `task build` | clean |

`TestProcE2E_ChangeIDPropagatesToSidecar` observed the change ID actually advancing
between snapshots, which is direct evidence for TMPL-08 rather than an assertion
that it should.

`task test:e2e:docker` was **not run** in this audit (requires Docker).

---

## Nyquist Coverage

| Phase | VALIDATION.md | status | Compliant | Classification |
|-------|---------------|--------|-----------|----------------|
| 01 | exists | `draft` | `false` | **NOT-VALIDATED** |

`nyquist_compliant: false` here is **not** a compliance failure. The file's `status`
is `draft`, meaning it was seeded by plan-phase and never reconciled by
validate-phase, so the boolean is not authoritative (workflow rule #2117). This is a
coverage TODO, not a blocker.

**Action if desired:** `/gsd-validate-phase 1` promotes the file to
`status: validated` and yields the real COMPLIANT/PARTIAL verdict.

---

## Tech Debt

All items are already recorded in STATE.md and were carried into PR #404's body —
none is newly discovered here.

**Phase 01:**

- UAT test 42 (resolver reload + two-node convergence) skipped — needs a real
  unbound host and a second machine. Recorded NOT-RUN in `01-VALIDATION.md`.
- Four manual deployment verifications from plan 01-08 recorded NOT-RUN.
- No e2e tier runs in `ci-go.yml` (#403). `proc_e2e` is the only tier that can catch
  CLI-flag regressions like G-01-1, and it is not gating merges. **Highest-value
  item here** — it is the control that would have caught the defect this milestone
  had to add two gap-closure plans to fix.
- `store.ListAll` still folds full event history server-side (#400, #401).
- `docs/reference/cli.md` hand-maintained; generated output fails rumdl (#402).
- `01-VALIDATION.md` still `status: draft`.

**Cross-cutting:**

- `service.go` (1033 LOC) and `commands.go` (519 LOC) are merge hotspots.
- `legacy_migration.go` is a permanent maintenance surface pending a removal milestone.
- `/gsd-ship`'s ship-note `[ci skip]` marker leaves this repo's required checks
  unreported on the PR head.

**Total:** 9 items across 2 groupings. None blocks the milestone.

---

## Verdict

No unsatisfied requirements, no orphans, no integration gaps, no broken flows.
Status is `tech_debt` rather than `passed` because the deferred set is real and
worth a decision — particularly #403, which leaves the milestone's own regression
control outside CI.
