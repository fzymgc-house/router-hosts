---
phase: 01-ci-gating-for-the-e2e-tiers
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/testutil/dockergate/dockergate.go
  - internal/testutil/dockergate/dockergate_test.go
  - e2e/docker_e2e_test.go
  - docs/contributing/testing.md
autonomous: true
requirements: [CI-04, VRFY-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "dockergate.Decide(envValue string, probeErr error) Action returns Fatal when the env value is non-empty and probeErr is non-nil, Skip when the env value is empty and probeErr is non-nil, and Proceed when probeErr is nil regardless of the env value."
    - "Both existing skip sites in requireDocker (e2e/docker_e2e_test.go:140 missing binary, :144 dead daemon) route through the same dockergate.Decide call — neither retains an unconditional t.Skip (D-06)."
    - "With RH_E2E_REQUIRE_DOCKER set and Docker unavailable, the docker_e2e tier fails via t.Fatalf naming the env var; with it unset and Docker unavailable, it still skips, so `task test:e2e:docker` stays usable for contributors without a daemon (D-06, CI-04)."
    - "The env var name RH_E2E_REQUIRE_DOCKER exists in exactly one place in Go source — dockergate.EnvVar — so the workflow YAML and the test code cannot drift to different spellings."
    - "waitForDockerServer's polling loop is gone: it calls wait.Until for the poll and dumps container logs from a t.Cleanup that fires only when t.Failed() (decision P-02), preserving today's diagnostic without adding a failure hook to the shared helper."
    - "e2e/docker_e2e_test.go contains no unmarked time.Sleep — every remaining occurrence, if any, is preceded within three lines by a SLEEP-INTENTIONAL: marker (D-16 structural gate)."
    - "docs/contributing/testing.md's 'CI Integration' section no longer states that none of the three e2e tiers run in CI, and documents RH_E2E_REQUIRE_DOCKER's skip-vs-fail semantics."
  artifacts:
    - "internal/testutil/dockergate/dockergate.go — tag-free, pure decision function for the Docker precondition gate"
    - "internal/testutil/dockergate/dockergate_test.go — unit tests covering all four env x probe combinations"
    - "e2e/docker_e2e_test.go — env-gated requireDocker and a wait.Until-based waitForDockerServer"
    - "docs/contributing/testing.md — corrected CI Integration section and RH_E2E_REQUIRE_DOCKER documentation"
  key_links:
    - "dockergate.EnvVar (Go) <-> the e2e-docker job's env: block in ci-go.yml (plan 04) — a typo on either side silently restores skip-as-success, which is exactly what CI-04 exists to prevent"
    - "dockergate is tag-free, so its unit tests run inside the existing Test job and gate every PR; the docker_e2e-tagged caller is exercised only by the e2e-docker job"
  prohibitions:
    - "MUST NOT leave an unconditional t.Skip on either Docker precondition path (D-06)."
    - "MUST NOT hard-fail unconditionally — with RH_E2E_REQUIRE_DOCKER unset the tier must still skip, so contributors without a daemon can run the rest of the suite (D-06)."
    - "MUST NOT add a failure hook, callback, or option surface to internal/testutil/wait for one caller's log dump (decision P-02)."
    - "MUST NOT spell RH_E2E_REQUIRE_DOCKER as a string literal anywhere in Go source outside dockergate.EnvVar's declaration."
    - "MUST NOT touch .github/workflows/ci-go.yml — plan 04 owns the e2e-docker job."
    - "MUST NOT add a //nolint directive without explicit user approval."
    - "MUST NOT write to the real filesystem in tests — use t.TempDir()."
    - "MUST NOT invoke `go test` directly where a `task` target exists."
---

<objective>
Make the containerized tier incapable of reporting green while testing nothing:
`requireDocker` hard-fails when `RH_E2E_REQUIRE_DOCKER` is set and Docker is
unreachable, and skips only when the operator has not asked for a hard
guarantee. Route the decision through a tag-free, unit-tested pure function so
the branch logic is covered by the existing `Test` job rather than by a tier
that might itself be skipped. Convert this file's remaining readiness poller and
correct the contributor docs that still say no e2e tier runs in CI.

Purpose: CI-04's first half. A green `e2e-docker` job must mean the Docker tests
actually ran.

Output: `internal/testutil/dockergate`, an env-gated `requireDocker`, a
helper-backed `waitForDockerServer`, and accurate testing docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ci-gating-for-the-e2e-tiers/01-CONTEXT.md
@.planning/phases/01-ci-gating-for-the-e2e-tiers/01-PATTERNS.md
@.planning/phases/01-ci-gating-for-the-e2e-tiers/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

Newly created symbols, paths, and identifiers introduced by phase 1. These do
not exist before this phase; do not flag them as drift.

| Artifact | Kind | Created by |
|----------|------|-----------|
| `internal/testutil/wait` + `wait.Until` + `wait.UntilValue` | Go package + exported funcs | plan 01 |
| `internal/ciwiring` | Go package path (new) | plan 01 |
| `e2e-fast` job id, `E2E_FAST_RESULT` env var, `workflow_dispatch` trigger | CI identifiers | plan 01 |
| `SLEEP-INTENTIONAL:` | source marker token | plan 02 |
| `internal/testutil/dockergate` | Go package path (new) | **plan 03** |
| `dockergate.EnvVar` (value `"RH_E2E_REQUIRE_DOCKER"`) | exported const | **plan 03** |
| `dockergate.Action` with `dockergate.Proceed`, `dockergate.Skip`, `dockergate.Fatal` | exported type + consts | **plan 03** |
| `dockergate.Decide(envValue string, probeErr error) Action` | exported func | **plan 03** |
| `RH_E2E_REQUIRE_DOCKER` | environment variable | **plan 03** |
| `e2e-docker`, `e2e-proc` job ids; `E2E_DOCKER_RESULT`, `E2E_PROC_RESULT` env vars | CI identifiers | plan 04 |

<flagged_assumptions>
**Unresolved probe row — CI-04 / `unclassified`.** The deterministic edge probe
returned one `unclassified` row for CI-04 with the generic instruction "review
manually". Per the spec-less fallback rules this row is NOT auto-`backstop`ped
and NOT dismissed; it is surfaced here as a flagged planner assumption for the
developer to accept or correct.

Manual review of CI-04 surfaced one genuine ambiguity the requirement text does
not settle, and the planner has assumed an answer:

> **Assumption A-01:** CI-04 says "the container tiers hard-fail when Docker is
> unavailable instead of skipping". D-06 narrows this to *env-gated* hard-fail.
> The planner assumes "Docker is unavailable" means exactly the two conditions
> `requireDocker` already probes — the `docker` binary is not on `PATH`, and
> `docker info` exits non-zero — and nothing more. A third failure mode (daemon
> reachable but `docker build` fails, e.g. no disk or no network to the
> registry) is NOT converted into a skip-vs-fail decision by this phase; it
> already surfaces as an ordinary test failure through `task test:e2e:docker`'s
> `docker:build` dependency, which is the desired behavior. If the developer
> intended CI-04 to also cover build-time unavailability, that is a scope
> addition, not a bug in this plan.

Verification disposition: **flagged-unverified**. This assumption is recorded,
not proven; it is not authored into `must_haves.truths` and must not be treated
as verified at UAT time.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the tag-free Docker precondition decision function</name>

  <read_first>
    - `e2e/docker_e2e_test.go` lines 130-150 — the current `requireDocker`, both `t.Skip` sites, and the surrounding helper conventions
    - `internal/storage/storagetest/suite.go` lines 1-20 — package-doc convention for an internal support package
    - `.golangci.yml` — `revive`'s `exported` rule (every exported identifier needs a doc comment) and `exhaustive` with `default-signifies-exhaustive: true` (a switch over `Action` needs a `default` arm or every constant)
    - `internal/testutil/wait/wait.go` — the sibling package's shape, so `internal/testutil/` stays internally consistent
    - `Taskfile.yml` lines 47-60 — `test:coverage:ci` enforces 80% over `./internal/...` and excludes only `/storagetest/`; this package is tag-free and fully unit-tested, so it stays inside the calculation
  </read_first>

  <files>internal/testutil/dockergate/dockergate.go, internal/testutil/dockergate/dockergate_test.go</files>

  <behavior>
    Write `dockergate_test.go` FIRST and watch it fail (RED), then implement
    `dockergate.go`. Required cases — all four combinations plus the boundary:

    - `Decide("", nil)` returns `Proceed` (Docker present, no hard requirement).
    - `Decide("1", nil)` returns `Proceed` (Docker present, hard requirement satisfied).
    - `Decide("", errors.New("not found"))` returns `Skip` (Docker absent, no hard requirement).
    - `Decide("1", errors.New("not found"))` returns `Fatal` (Docker absent, hard requirement violated).
    - `Decide("0", errors.New("x"))` returns `Fatal` — the gate is presence-based, not truthiness-based: any non-empty value means required. This is the boundary the CI job's `RH_E2E_REQUIRE_DOCKER: "1"` depends on, and it must be pinned by a test so nobody "helpfully" adds strconv.ParseBool later.
    - `Decide("  ", errors.New("x"))` returns `Fatal` — whitespace is a non-empty value; no trimming.
    - `EnvVar` equals the exact string `RH_E2E_REQUIRE_DOCKER`.
    - `Action`'s zero value is `Proceed`, so a forgotten assignment fails open to "run the test", never to "silently skip".
  </behavior>

  <action>
Create package `dockergate` at `internal/testutil/dockergate/`. No build tag, no
`testing` import — this is a pure function so the branch logic is covered by the
existing `Test` CI job rather than by a tier that could itself be skipped
(RESEARCH.md's Wave 0 gap: `requireDocker`'s branch logic has zero coverage
today because it lives behind `//go:build docker_e2e`).

Declare:
- `const EnvVar = "RH_E2E_REQUIRE_DOCKER"` — the single source of truth for the
  variable name. The workflow YAML in plan 04 sets this exact string; nothing
  else in Go source may spell it as a literal.
- `type Action int` with `Proceed Action = iota`, then `Skip`, then `Fatal`.
  `Proceed` must be the zero value so a forgotten assignment fails toward
  running the test.
- `func Decide(envValue string, probeErr error) Action` — returns `Proceed` when
  `probeErr == nil`; otherwise `Fatal` when `envValue != ""` and `Skip` when it
  is empty. Presence-based, not truthiness-based: do not call
  `strconv.ParseBool`, do not trim whitespace.

Every exported identifier needs a doc comment (`revive`'s `exported` rule). The
package doc should state why this lives outside the build-tagged test file: so
the skip-vs-fail decision is unit-testable without Docker and without the
`docker_e2e` tag.

Do not import `os` here — the caller passes `os.Getenv(dockergate.EnvVar)` in.
Keeping `Decide` free of ambient state is what makes all seven cases testable.

Commit as two atomic commits: `test(e2e): add failing tests for docker gate`
then `feat(e2e): add tag-free docker precondition gate`.
  </action>

  <verify>
    <automated>go test ./internal/testutil/dockergate/... && golangci-lint run ./internal/testutil/... && task test:coverage:ci</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/testutil/dockergate/...` passes, with a test function or subtest for each of the eight cases in `<behavior>`.
    - Before the implementation commit, `go test ./internal/testutil/dockergate/...` was observed FAILING (RED); record the failure output in the SUMMARY.
    - `rg -n 'RH_E2E_REQUIRE_DOCKER' --type go .` returns exactly one match in non-test Go source, at `internal/testutil/dockergate/dockergate.go`'s `EnvVar` declaration. (Matches inside `dockergate_test.go` asserting the constant's value are expected and permitted.)
    - `rg -n 'ParseBool|strings\.TrimSpace' internal/testutil/dockergate/dockergate.go` returns no match — the gate is presence-based.
    - `rg -n '"os"' internal/testutil/dockergate/dockergate.go` returns no match — `Decide` reads no ambient state.
    - `go tool cover -func` over this package shows 100% statement coverage of `Decide`.
    - `task test:coverage:ci` still passes the 80% threshold.
    - `golangci-lint run ./internal/testutil/...` reports 0 issues.
    - `go list -deps ./cmd/router-hosts/... | rg 'testutil/dockergate'` and `go list -deps ./cmd/operator/... | rg 'testutil/dockergate'` both exit non-zero (no match).
  </acceptance_criteria>

  <done>
    The skip-vs-fail decision is a pure, fully covered function that runs in the
    `Test` job on every PR, independent of whether any Docker tier ran.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Env-gate requireDocker and route waitForDockerServer through the shared helper</name>

  <read_first>
    - `e2e/docker_e2e_test.go` lines 1-30 — build tag `//go:build docker_e2e`, the import block (note whether `"os"` is already imported), and the `startupTimeout = 30 * time.Second` const
    - `e2e/docker_e2e_test.go` lines 36-135 — the three tests (`TestDockerE2E_ImageBuildsAndServes` at 41, `TestDockerE2E_WrongCARejected` at 94, `TestDockerE2E_OperatorBinaryExists` at 118) and their `requireDocker(t)` calls at lines 42, 95, 119
    - `e2e/docker_e2e_test.go` lines 136-150 — `requireDocker`'s current body with both `t.Skip` sites (140, 144)
    - `e2e/docker_e2e_test.go` lines 250-285 — `waitForDockerServer`, its container-log dump on both the "container exited" branch and the final timeout, and the `time.Sleep(500 * time.Millisecond)` at line 276
    - `internal/testutil/dockergate/dockergate.go` — `EnvVar`, `Action`, `Decide` as built in task 1
    - `internal/testutil/wait/wait.go` — `Until`'s signature
    - `.planning/phases/01-ci-gating-for-the-e2e-tiers/01-02-PLAN.md` `<planner_decisions>` — decision P-02, which this task implements
  </read_first>

  <files>e2e/docker_e2e_test.go</files>

  <behavior>
    - With `RH_E2E_REQUIRE_DOCKER` unset and `docker` absent from `PATH`, all three `TestDockerE2E_*` tests skip (today's behavior for contributors without a daemon).
    - With `RH_E2E_REQUIRE_DOCKER` set to any non-empty value and `docker` absent from `PATH`, all three tests fail with a message naming `RH_E2E_REQUIRE_DOCKER` — they never report skipped or passed.
    - With `RH_E2E_REQUIRE_DOCKER` set and the `docker` binary present but the daemon unreachable (`docker info` non-zero), the same hard failure occurs — both precondition paths behave identically.
    - `waitForDockerServer` still fails fast with the container's logs when the container exits before becoming ready, and still fails with the container's logs on timeout.
    - `task test:e2e:docker` passes on a machine with a working Docker daemon.
  </behavior>

  <action>
Rewrite two functions in `e2e/docker_e2e_test.go`. Import
`"github.com/fzymgc-house/router-hosts/internal/testutil/dockergate"`,
`"github.com/fzymgc-house/router-hosts/internal/testutil/wait"`, and `"os"` if
not already present.

**`requireDocker(t *testing.T)` (currently lines 136-146).** Keep the signature
and `t.Helper()`. Probe once for the binary: `_, lookErr := exec.LookPath("docker")`.
Switch on `dockergate.Decide(os.Getenv(dockergate.EnvVar), lookErr)`: on `Fatal`
call `t.Fatalf` with a message naming `dockergate.EnvVar`, the fact that it is
set, and the underlying error; on `Skip` call `t.Skip` with today's wording
("docker not found, skipping Docker E2E test"); on `Proceed` fall through. Then
probe the daemon: `infoErr := exec.Command("docker", "info").Run()` and switch on
`dockergate.Decide(os.Getenv(dockergate.EnvVar), infoErr)` with the analogous
Fatal/Skip/Proceed arms and today's daemon wording. Both skip sites therefore
route through the same gate, which is D-06's explicit requirement. Because
`exhaustive` is enabled with `default-signifies-exhaustive: true`, each switch
needs either all three `Action` constants or a `default` arm — prefer explicit
`case dockergate.Fatal:` / `case dockergate.Skip:` with a `default:` that falls
through to proceeding. Update the function's doc comment: it no longer only
skips.

**`waitForDockerServer(t *testing.T, env *dockerEnv)` (currently lines 252-280),
implementing decision P-02.** Register the log dump as a cleanup at the top of
the function: `t.Cleanup(func() { if t.Failed() { logs, _ := exec.Command("docker",
"logs", env.containerID).CombinedOutput(); t.Logf("container %s logs:\n%s",
env.containerID, string(logs)) } })`. This preserves the diagnostic on every
failure path — including the shared helper's timeout `Fatalf` — without adding a
failure hook, callback, or option to `internal/testutil/wait` for one caller.
Then replace the deadline loop with a single `wait.Until`: timeout
`startupTimeout`, interval `500 * time.Millisecond`, desc naming the container
and the gRPC address (e.g. `"container <id> to answer Liveness on <addr>"`), and
a condition closure that keeps today's body: run `docker inspect -f
{{.State.Running}}`, and if that errors or does not report `true` call
`t.Fatalf("container exited before becoming ready")` from inside the closure —
this is safe and correct because the closure runs on the test's own goroutine;
otherwise dial, call `Liveness` with a 2s context, close the connection, and
return whether the call succeeded. Delete both inline `docker logs` invocations
and the trailing `t.Fatalf` — the cleanup and the helper now own them.

Keep `caCertPEM := mustReadFile(t, env.caCertPath)` outside the closure so the
file is read once, not once per poll.

Commit as two atomic commits: `feat(e2e): hard-fail docker tier when required`
then `test(e2e): route docker readiness through wait helper`.
  </action>

  <verify>
    <automated>task test:e2e:docker && awk 'FNR==1{mark=-100} /SLEEP-INTENTIONAL:/{mark=FNR} /time\.Sleep\(/{ if (FNR-mark>3) { print FILENAME":"FNR; bad=1 } } END{exit bad+0}' e2e/docker_e2e_test.go && golangci-lint run --build-tags docker_e2e ./e2e/...</automated>
  </verify>

  <acceptance_criteria>
    - `task test:e2e:docker` passes on a machine with a working Docker daemon.
    - **Local hard-fail proof (this is the code-level half of D-10's fourth negative control; the CI-level half is plan 05's):** run `env RH_E2E_REQUIRE_DOCKER=1 PATH=/usr/bin:/bin go test -tags docker_e2e -run TestDockerE2E_OperatorBinaryExists ./e2e/` with `docker` removed from `PATH`, and observe `FAIL` with a message containing `RH_E2E_REQUIRE_DOCKER` — not `SKIP`, not `ok`. Record the output in the SUMMARY. Then run the same command WITHOUT `RH_E2E_REQUIRE_DOCKER` set and observe the test skipping. Both observations are required; the first is the red proof of the new gate, the second proves the contributor path still works.
    - `rg -n 't\.Skip\(' e2e/docker_e2e_test.go` returns exactly two matches, both inside `requireDocker`, and each is inside a `case dockergate.Skip:` arm — `rg -n -B2 't\.Skip\(' e2e/docker_e2e_test.go` shows `dockergate.Skip` above each.
    - `rg -n 'dockergate\.Decide' e2e/docker_e2e_test.go` returns exactly two matches, one per precondition probe (D-06: both skip sites route through the same gate).
    - `rg -n '"RH_E2E_REQUIRE_DOCKER"' e2e/docker_e2e_test.go` returns no match — the name is referenced only as `dockergate.EnvVar`.
    - `rg -n 'time\.Sleep' e2e/docker_e2e_test.go` returns no match, and the structural marker gate over this file exits 0. Before editing, run the gate and record the one line it prints (276) in the SUMMARY as this task's red observation.
    - `rg -n 'internal/testutil/wait|internal/testutil/dockergate' e2e/docker_e2e_test.go` matches both imports.
    - `rg -n 't\.Cleanup' e2e/docker_e2e_test.go` matches inside `waitForDockerServer`, and `rg -n 't\.Failed\(\)' e2e/docker_e2e_test.go` matches inside that cleanup — the log dump is failure-gated, not unconditional.
    - `rg -n 'docker.*logs' internal/testutil/wait/wait.go` returns no match — no Docker-specific concern leaked into the shared helper (decision P-02).
    - `golangci-lint run --build-tags docker_e2e ./e2e/...` reports 0 issues.
    - `git diff --stat` for this task touches only `e2e/docker_e2e_test.go`.
  </acceptance_criteria>

  <precondition>A working Docker daemon is reachable (`docker info` exits 0) for the passing half of this task's verification; the hard-fail proof additionally requires being able to run a command with `docker` removed from `PATH`.</precondition>

  <done>
    A green `e2e-docker` run can no longer mean "Docker was missing so we
    skipped", the tier still skips gracefully for contributors without a daemon,
    and the container-log diagnostic survives the poller rewrite.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the contributor testing docs</name>

  <read_first>
    - `docs/contributing/testing.md` lines 236-251 — the entire `## CI Integration` section, which currently asserts "None of the three e2e tiers currently run in CI" and cites issue #403 as an open limitation
    - `docs/contributing/testing.md` lines 28-50 — the "Running E2E Tests" section and the per-tier external-dependency notes, where `RH_E2E_REQUIRE_DOCKER` belongs
    - `docs/contributing/testing.md` lines 165-175 — the harness building-blocks list, which describes `waitForFileContent` / `waitForSidecar` as "filesystem-based polling helpers"
    - `.github/workflows/ci-go.yml` — the current job set, so the doc names the real job ids and check name
    - `Taskfile.yml` lines 27-40 — the three `test:e2e*` targets the doc references
  </read_first>

  <files>docs/contributing/testing.md</files>

  <action>
Replace the `## CI Integration` section's claim that no e2e tier runs in CI.
The replacement must state, concretely:

- All three tiers run on every pull request as three parallel jobs: `e2e-fast`
  (runs `task test:e2e`), `e2e-docker` (runs `task test:e2e:docker`), and
  `e2e-proc` (runs `task test:e2e:proc` after removing `bin/`).
- Their results feed the single aggregated required check `CI (Go) Complete`
  via the `ci-go-complete` job; a tier that fails, is cancelled, or is skipped
  makes that check fail, so a merge cannot land without all three green.
- There is no merge queue in this repository and merges are squash-only, so the
  pull-request check *is* the merge gate — the fast tier and the slow tiers gate
  at the same moment rather than in two stages.
- `internal/ciwiring`'s test asserts that every `e2e-*` job is represented in
  the aggregator, so adding a fourth tier without wiring it in fails `task test`.
- Issue #403 is closed by this work; keep the reference but reframe it as
  resolved rather than as an open limitation. Keep the G-01-1 explanation of
  *why* the tiers must run, as motivation, in past tense.

Add documentation for `RH_E2E_REQUIRE_DOCKER` in the "Running E2E Tests" area:
when unset (the contributor default), `task test:e2e:docker` skips if Docker is
missing or the daemon is down; when set to any non-empty value — which the
`e2e-docker` CI job does — the same conditions fail the test instead of skipping
it, so a green CI run cannot mean "skipped". Note that presence, not
truthiness, is what counts: `RH_E2E_REQUIRE_DOCKER=0` still means required.

Update the harness building-blocks bullet so `waitForFileContent` /
`waitForSidecar` are described as thin wrappers over
`internal/testutil/wait.UntilValue`, and add a short note that all readiness
waiting across the three tiers goes through `internal/testutil/wait` — with the
one deliberate exception in `e2e/e2e_test.go` carrying a `SLEEP-INTENTIONAL:`
marker, which exists so a future sweep does not remove a sleep that is holding
an outage window open.

Run `rumdl` (the repo's markdown linter, per CLAUDE.md's prerequisites) over the
file and fix any findings properly rather than suppressing them.

Commit as `docs(e2e): document CI gating and RH_E2E_REQUIRE_DOCKER`.
  </action>

  <verify>
    <automated>rumdl check docs/contributing/testing.md && task docs:ci BINARY=/tmp/router-hosts</automated>
  </verify>

  <acceptance_criteria>
    - `rg -n 'None of the three e2e tiers currently run in CI' docs/contributing/testing.md` returns no match.
    - `rg -n 'e2e-fast' docs/contributing/testing.md`, `rg -n 'e2e-docker' docs/contributing/testing.md`, and `rg -n 'e2e-proc' docs/contributing/testing.md` each match.
    - `rg -n 'CI \(Go\) Complete' docs/contributing/testing.md` matches.
    - `rg -n 'RH_E2E_REQUIRE_DOCKER' docs/contributing/testing.md` matches at least twice: once describing the unset/skip behavior and once describing the set/fail behavior.
    - `rg -n 'SLEEP-INTENTIONAL' docs/contributing/testing.md` matches, so a contributor encountering the marker can find out what it means.
    - `rg -n 'internal/testutil/wait' docs/contributing/testing.md` matches.
    - `rumdl check docs/contributing/testing.md` reports no findings.
    - `task docs:ci BINARY=/tmp/router-hosts` succeeds (build the binary first with `go build -o /tmp/router-hosts ./cmd/router-hosts`), matching what the `docs` CI job runs — a broken link or heading in this file must not break that job.
    - No `//nolint`-equivalent suppression (e.g. a `rumdl` disable comment) is added.
  </acceptance_criteria>

  <done>
    A contributor reading `docs/contributing/testing.md` learns that all three
    tiers gate every PR, what `RH_E2E_REQUIRE_DOCKER` does, and why one sleep in
    the suite carries a marker instead of being converted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner environment -> test precondition guard | The runner decides whether Docker exists; `requireDocker` decides whether that absence is fatal |
| workflow YAML env var name -> Go source env var name | Two independently-edited files must agree on one string or the gate silently reverts to skip-as-success |
| container runtime -> test diagnostics | When the container fails to start, its logs are the only evidence of why |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Repudiation | `requireDocker`'s `t.Skip` paths | high | mitigate | `dockergate.Decide` makes the skip conditional on `RH_E2E_REQUIRE_DOCKER` being unset; the CI job sets it (plan 04). Proven locally by running the tier with the env var set and `docker` removed from `PATH` and observing FAIL rather than SKIP, and proven in CI by D-10's fourth negative control (plan 05). |
| T-01-08 | Tampering | env var name drift between YAML and Go | high | mitigate | The name exists once in Go source, as `dockergate.EnvVar`. An acceptance criterion asserts `rg -n 'RH_E2E_REQUIRE_DOCKER' --type go .` finds exactly one non-test declaration. Plan 04's job block sets the same literal, and plan 05's negative control is the end-to-end proof that both sides agree. |
| T-01-09 | Information disclosure (loss of) | `waitForDockerServer`'s log dump | medium | mitigate | The dump moves into a `t.Cleanup` guarded by `t.Failed()`, so it fires on every failure path including the shared helper's timeout `Fatalf` — strictly more coverage than today's two hand-placed dumps, with no unconditional log spew on success. |
| T-01-10 | Repudiation | stale contributor documentation | low | mitigate | Task 3 corrects the "no e2e tier runs in CI" claim in the same phase that makes it false, with `rg` criteria asserting both the removal of the stale sentence and the presence of the new job ids. |
| T-01-SC | Tampering (supply chain) | Go module graph | low | accept | This plan adds no packages and makes no `go.mod` change; `internal/testutil/dockergate` is stdlib-free beyond the `error` type. RESEARCH.md's Package Legitimacy Audit is N/A for this phase. |
</threat_model>

<verification>
- `go test ./internal/testutil/dockergate/...` passes with 100% coverage of `Decide`
- `task test:e2e:docker` passes with a daemon present
- The tier FAILS (not skips) with `RH_E2E_REQUIRE_DOCKER=1` and `docker` off `PATH`; it SKIPS with the var unset
- Structural marker gate over `e2e/docker_e2e_test.go` exits 0 and prints nothing
- `task lint` passes; `rumdl check docs/contributing/testing.md` reports no findings
- `task test:coverage:ci` still clears 80%
</verification>

<success_criteria>
- `dockergate.Decide` exists, is tag-free, and is fully unit-tested including the presence-not-truthiness boundary
- Both `requireDocker` skip sites route through the same gate (D-06)
- `waitForDockerServer` has no loop of its own and keeps its container-log diagnostic via `t.Cleanup` (decision P-02)
- `docs/contributing/testing.md` no longer claims the e2e tiers do not run in CI
</success_criteria>

<output>
Create `.planning/phases/01-ci-gating-for-the-e2e-tiers/01-03-SUMMARY.md` when done.
Record in it: the RED output for `dockergate_test.go`, the local hard-fail proof
output (FAIL with `RH_E2E_REQUIRE_DOCKER` in the message) alongside the
skip-when-unset output, and the pre-edit marker-gate output for
`e2e/docker_e2e_test.go`.
</output>
