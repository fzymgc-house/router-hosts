---
phase: 01-consumer-rendered-output-templates-sink
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-04
files_modified:
  - internal/config/client.go
  - internal/config/client_test.go
  - internal/client/client.go
  - internal/client/client_test.go
  - internal/client/commands/host.go
  - internal/client/commands/host_test.go
  - internal/client/commands/snapshot.go
  - internal/client/commands/snapshot_test.go
  - internal/client/commands/render.go
  - internal/client/commands/render_test.go
  - docs/reference/configuration.md
autonomous: true
requirements:
  - TMPL-07
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every client-side loop that accumulates stream messages into a slice stops at a configurable limit (TMPL-07, D-14)"
    - "Exceeding the limit returns an error naming the limit and how to raise it, and never returns a truncated result (D-14)"
    - "The limit has a safe default that requires no configuration to be protective"
    - "The limit is configurable through the client TOML file and an environment variable, so it is not an unfixable compiled-in ceiling"
  artifacts:
    - internal/config/client.go
    - internal/client/client.go
    - internal/client/commands/host.go
  key_links:
    - "config.ClientConfig.Limits.MaxStreamEntries flows through client.NewClient into Client.MaxStreamEntries() and from there into every collecting call site"
    - "NewClientFromConn (bufconn test path) falls back to DefaultMaxStreamEntries so tests exercise the same bounded path as production"
---

# Phase 1 Plan 03 — Bounded, Fail-Loud Client Stream Collection

<objective>
Close TMPL-07: a malicious or buggy server can currently drive the CLI out of memory
because `collectHostStream`, `collectSearchStream` and `collectSnapshotStream` all
`append` without any ceiling, and the new `render` drain loop inherits the same
shape.

Purpose: D-15 makes this load-bearing rather than theoretical — because `.Count` and
`.GeneratedAt` are part of the contract, the client must hold the whole entry set
before rendering, so the cap is the only thing bounding it.
Output: a configurable, fail-loud collection limit applied at every collecting call
site, with a safe default.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-CONTEXT.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-RESEARCH.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-PATTERNS.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-01-SUMMARY.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this plan produces

- `internal/config`: `ClientLimitsConfig` type, `ClientConfig.Limits` field,
  `EnvMaxStreamEntries` constant, `DefaultMaxStreamEntries` constant
- `internal/client`: `(*Client).MaxStreamEntries` method
- `internal/client/commands`: `errStreamLimitExceeded` constructor (or equivalent
  named error helper), `limit` parameter added to `collectHostStream`,
  `collectSearchStream`, `collectSnapshotStream`
- Config surface: `[limits] max_stream_entries` TOML key,
  `ROUTER_HOSTS_MAX_STREAM_ENTRIES` environment variable

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configurable collection limit reaches every client</name>
  <files>internal/config/client.go, internal/config/client_test.go, internal/client/client.go, internal/client/client_test.go</files>
  <read_first>
- internal/config/client.go (the whole file — struct shape, env constants, overrides, validate, the strict `meta.Undecoded()` unknown-key rejection at lines 102-108)
- internal/config/client_test.go (existing test style for config layering)
- internal/client/client.go (the whole file — `Client` struct, `NewClient`, `NewClientFromConn`)
- internal/client/client_test.go (existing construction tests)
- .planning/phases/01-consumer-rendered-output-templates-sink/01-CONTEXT.md (D-14: configurable with a safe default; a fixed compiled-in constant was rejected)
  </read_first>
  <behavior>
- A client config file containing a `[limits]` table with `max_stream_entries = 10` loads with that value and is not rejected as an unknown key.
- A config with no `[limits]` table yields the default limit rather than zero.
- Setting `ROUTER_HOSTS_MAX_STREAM_ENTRIES=25` overrides the file value.
- A non-numeric or negative environment value is rejected by config validation with a clear error rather than silently falling back.
- A `Client` built by `NewClient` reports the configured limit; a `Client` built by `NewClientFromConn` reports the default.
  </behavior>
  <action>
1. `internal/config/client.go` — add `DefaultMaxStreamEntries = 50_000` as an
exported constant with a doc comment explaining it is an entry count (not a byte
budget) chosen because `.Count` is a first-class contract field, and that a
deployment with a larger inventory raises it rather than being blocked by a
compiled-in ceiling. Add `type ClientLimitsConfig struct { MaxStreamEntries int
\`toml:"max_stream_entries"\` }` and a `Limits ClientLimitsConfig \`toml:"limits"\``
field on `ClientConfig`. Add `EnvMaxStreamEntries = "ROUTER_HOSTS_MAX_STREAM_ENTRIES"`
to the existing env constant block. In `applyClientEnv`, when that variable is
set, parse it with `strconv.Atoi` and assign; on a parse failure record the raw
value so `validate` can reject it — do not silently ignore a malformed value.
In `LoadClientConfig`, after the override layer and before `validate`, substitute
`DefaultMaxStreamEntries` when the resolved value is zero. In `validate`, return
`oops.Code(domain.CodeValidation).Errorf(...)` when the resolved value is
negative or when the env variable was present but unparseable. Also add
`MaxStreamEntries *int` to `ClientConfigOverrides` and honour it in
`applyClientOverrides`, so a future flag can override without another change here.

2. `internal/client/client.go` — add an unexported `maxStreamEntries int` field
to `Client` and an exported `func (c *Client) MaxStreamEntries() int` accessor
that returns `config.DefaultMaxStreamEntries` when the field is zero. Populate
the field from `cfg.Limits.MaxStreamEntries` in `NewClient`. Leave
`NewClientFromConn` populating nothing, so the accessor's zero-value fallback
gives bufconn-backed test clients the same protective default as production.

3. Tests. `internal/config/client_test.go`: add
`TestLoadClientConfig_LimitsFromFile`, `TestLoadClientConfig_LimitsDefault`,
`TestLoadClientConfig_LimitsFromEnv`, `TestLoadClientConfig_LimitsInvalid`,
following the existing tests' use of `t.Setenv` and `t.TempDir()`.
`internal/client/client_test.go`: add `TestClient_MaxStreamEntriesFromConfig` and
`TestClient_MaxStreamEntriesDefaultsFromConn`.

Commit as `feat(config): add client stream collection limit`.
  </action>
  <verify>
<automated>task test -- -run 'TestLoadClientConfig_Limits' ./internal/config/ &amp;&amp; task test -- -run 'TestClient_MaxStreamEntries' ./internal/client/ &amp;&amp; task lint</automated>
  </verify>
  <acceptance_criteria>

- `task test -- -run 'TestLoadClientConfig_Limits' -v ./internal/config/` reports 4 PASS lines and 0 FAIL.
- `task test -- -run 'TestClient_MaxStreamEntries' -v ./internal/client/` reports 2 PASS lines and 0 FAIL.
- `rg -n 'DefaultMaxStreamEntries' internal/config/client.go internal/client/client.go` returns at least 3 lines (declaration plus both fallbacks).
- `task test ./internal/config/ ./internal/client/` is green — the strict unknown-key rejection at `client.go:102-108` still fires for genuinely unknown keys, proven by the pre-existing test for that behavior still passing.
- `task lint` exits 0.
  </acceptance_criteria>
  <done>Every constructed `Client` carries a positive collection limit, defaulting safely and configurable by file, env, or override.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound every collecting stream loop, fail loud past the limit</name>
  <files>internal/client/commands/host.go, internal/client/commands/host_test.go, internal/client/commands/snapshot.go, internal/client/commands/snapshot_test.go, internal/client/commands/render.go, internal/client/commands/render_test.go, docs/reference/configuration.md</files>
  <read_first>
- internal/client/commands/host.go (lines 328-378 — `commandContext`, `collectHostStream`, `collectSearchStream`, and every call site of the two collectors)
- internal/client/commands/snapshot.go (lines 205-225 — `collectSnapshotStream` and its call site)
- internal/client/commands/render.go (the drain loop written in plan 01 and gated in plan 02)
- internal/client/commands/importexport.go (lines 105-116 — confirm whether its response drain accumulates; it currently keeps only the last response)
- internal/server/service.go (lines 436-460 — `maxImportBytes` and the ResourceExhausted refusal, the server-side precedent for the message shape)
- internal/client/commands/testhelper_test.go (`setupCmdTest`, the harness the new tests build on)
- .planning/phases/01-consumer-rendered-output-templates-sink/01-PATTERNS.md (section "Bounded, fail-loud collection")
  </read_first>
  <behavior>
- A stream yielding exactly `limit` entries collects successfully and returns all of them.
- A stream yielding `limit + 1` entries returns a non-nil error and a nil slice — never a slice of length `limit`.
- The returned error message contains the numeric limit, the TOML key name, and the environment variable name.
- `host list`, `host search`, `snapshot list` and `render` all surface that error to the caller rather than proceeding with partial data.
- `render` writes nothing to `--out` when the limit is exceeded, leaving any pre-existing artifact byte-identical.
  </behavior>
  <action>
1. `internal/client/commands/host.go` — add an unexported helper that produces
the refusal error, e.g. `func streamLimitError(limit int) error` returning
`oops.Errorf` whose message states that the server sent more than `limit`
entries, that the result was refused rather than truncated, and that the limit is
raised via the `limits.max_stream_entries` client-config key or the
`ROUTER_HOSTS_MAX_STREAM_ENTRIES` environment variable. Model the message on the
server-side `maxImportBytes` refusal at `service.go:458-460`, but return a plain
`oops` error rather than a gRPC status — the client is refusing to accumulate a
response it already received, not rejecting an inbound RPC.

2. Change `collectHostStream` and `collectSearchStream` to take a trailing
`limit int` parameter. Inside each loop, before appending, refuse when the slice
already holds `limit` items by returning `nil, streamLimitError(limit)`. Do the
same in `collectSnapshotStream` in `snapshot.go`. Update every call site to pass
`c.MaxStreamEntries()` from the `*client.Client` already in scope in that `RunE`.

3. `internal/client/commands/render.go` — apply the identical guard to the
`WatchHosts` drain loop: refuse before appending the entry that would exceed the
limit, returning `streamLimitError`. The refusal must happen before any call to
`template.Render` and therefore before any artifact write.

4. Confirm no other unbounded accumulation remains: run
`rg -n 'append\(' internal/client/commands/` and inspect every hit that sits
inside a `stream.Recv()` loop. Bound any that are found. If `importexport.go`'s
response drain turns out to keep only the most recent response (as the current
code appears to), record that in the plan summary as a finding rather than
inventing a change — GH #38's line references are stale and 01-CONTEXT.md says to
trust the code over the issue text.

5. Tests. `internal/client/commands/host_test.go`: add
`TestCollectHostStream_CapExceeded` and `TestCollectHostStream_AtCapSucceeds`
driving the real bufconn harness with the client's limit forced to a small value.
`internal/client/commands/snapshot_test.go`: add
`TestCollectSnapshotStream_CapExceeded`. `internal/client/commands/render_test.go`:
add `TestRender_CapExceededPreservesArtifact` asserting both the error and the
byte-identical pre-existing artifact. Because `setupCmdTest` builds its client
through `NewClientFromConn`, the tests need a way to force a small limit — add a
test-only override to the harness (an exported-for-test setter, a package
variable the harness assigns, or by having the harness construct the client
through a seam) rather than seeding tens of thousands of host entries.

6. `docs/reference/configuration.md` — document the `[limits]` table with
`max_stream_entries`, its default, its unit (entry count), the environment
variable, and the refusal behavior, matching the surrounding table style.

Commit as `fix(client): bound stream collection with fail-loud cap`.
  </action>
  <verify>
<automated>task test -- -run 'TestCollectHostStream_|TestCollectSnapshotStream_|TestRender_CapExceeded' ./internal/client/commands/ &amp;&amp; task test ./internal/client/... &amp;&amp; task lint</automated>
  </verify>
  <acceptance_criteria>

- `task test -- -run 'TestCollectHostStream_' -v ./internal/client/commands/` reports 2 PASS lines and 0 FAIL.
- `task test -- -run 'TestCollectSnapshotStream_' -v ./internal/client/commands/` reports 1 PASS line and 0 FAIL.
- `task test -- -run 'TestRender_CapExceeded' -v ./internal/client/commands/` reports 1 PASS line and 0 FAIL.
- `rg -c 'MaxStreamEntries\(\)' internal/client/commands/` returns at least 4 — one per collecting call site (host list, host search, snapshot list, render).
- Every collector signature carries the limit: `rg -n 'func collectHostStream|func collectSearchStream|func collectSnapshotStream' internal/client/commands/` returns 3 lines and each contains `limit int`.
- The cap-exceeded test asserts a nil slice, not a slice of length `limit` — confirm by reading the assertion, which must require the returned entries to be empty.
- `rg -c 'max_stream_entries' docs/reference/configuration.md` returns at least 1.
- `task test ./internal/client/...` is green with no pre-existing test modified except to pass the new limit argument.
- `task lint` exits 0 and `git diff` adds no line matching `nolint`.
  </acceptance_criteria>
  <done>No client loop can be driven past its configured entry ceiling; crossing it produces an actionable error and no partial result.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server stream to client memory | Every response message the client accumulates is attacker-controllable in volume by a compromised or buggy server |
| environment and config file to limit value | The ceiling protecting client memory is itself operator-supplied input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-05 | Denial of Service | unbounded `append` in `collectHostStream`, `collectSearchStream`, `collectSnapshotStream`, and the `render` drain loop | high | mitigate | A per-call entry ceiling defaulting to 50,000, checked before each append, returning a nil slice plus a named error. This is the closure of the threat plan 01 opened and deferred. |
| T-1-09 | Tampering | a limit value that disables the protection (zero, negative, or unparseable) | medium | mitigate | Zero resolves to the safe default rather than to "unlimited"; negative values and unparseable env values are rejected by `ClientConfig.validate` with a validation-coded `oops` error, so the process refuses to start rather than running unprotected. |
| T-1-10 | Denial of Service | silent truncation masking a real inventory that outgrew the limit | medium | mitigate | The refusal returns a nil slice and an error naming both the limit and the two ways to raise it (D-14 forbids silent truncation); tests assert the empty result explicitly. |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable: no package-manager install task exists in this plan and 01-RESEARCH.md's Package Legitimacy Audit records zero new external packages. |

</threat_model>

<verification>
- `task test ./internal/config/ ./internal/client/...` green.
- `task lint` clean.
- A repository-wide sweep for accumulation inside `stream.Recv()` loops finds no remaining unbounded case in `internal/client`.

</verification>

<success_criteria>

- All four collecting call sites refuse past the configured limit with an actionable error.
- The default protects a client that has configured nothing.
- The limit is raisable by config file, env var, or programmatic override.
- No collector can return a truncated result.
</success_criteria>

<output>
Create `.planning/phases/01-consumer-rendered-output-templates-sink/01-03-SUMMARY.md` when done
</output>
