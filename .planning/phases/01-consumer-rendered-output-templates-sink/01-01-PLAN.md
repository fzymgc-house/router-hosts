---
phase: 01-consumer-rendered-output-templates-sink
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/router_hosts/v1/hosts.proto
  - api/v1/router_hosts/v1/hosts.pb.go
  - api/v1/router_hosts/v1/hosts_grpc.pb.go
  - internal/server/watch.go
  - internal/server/watch_test.go
  - internal/client/template/template.go
  - internal/client/template/template_test.go
  - internal/client/commands/render.go
  - internal/client/commands/render_test.go
  - internal/client/commands/root.go
  - internal/atomicfile/atomicfile.go
  - internal/atomicfile/atomicfile_test.go
  - internal/server/hostsfile.go
  - internal/server/hostsfile_test.go
  - internal/server/unboundconf.go
  - internal/server/dnsmasqconf.go
autonomous: false
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A caller runs router-hosts render --template ./x.tmpl and gets host data rendered through their own template with no change to this repo (TMPL-01, D-01)"
    - "The template top-level value is a struct exposing .Entries plus .Count, .GeneratedAt and .ContractVersion, never a bare slice (D-03)"
    - "Each entry exposes IPAddress, Hostname, Aliases, Tags and Comment (TMPL-02, D-04)"
    - "A template referencing a field that does not exist fails with a Go error instead of rendering empty (TMPL-03)"
    - "--out writes the artifact through temp-file-plus-rename so a concurrent reader never observes a partial file (TMPL-04)"
    - "A render failure leaves an existing --out artifact byte-identical (D-12)"
    - "The three existing server generators still write through the relocated atomic-write helper with no observable change (phase success criterion 6)"
  artifacts:
    - proto/router_hosts/v1/hosts.proto
    - internal/server/watch.go
    - internal/client/template/template.go
    - internal/client/commands/render.go
    - internal/atomicfile/atomicfile.go
  key_links:
    - "render command to WatchHosts(follow=false) to store.ListAll to per-entry stream.Send to SnapshotComplete terminator"
    - "template.Data.ContractVersion is populated from SnapshotComplete.contract_version served by the server, not from a client-side constant"
    - "internal/atomicfile.Write is the single write-and-rename implementation used by both internal/server generators and the client render path"
---

# Phase 1 Plan 01 — Tracer — End-to-End Consumer-Rendered Output

<objective>
Prove the whole consumer-rendered-output architecture end to end on one thin path: a
caller supplies a `text/template` file, the CLI opens a new `WatchHosts` stream, the
server streams host entries plus a snapshot terminator, and the CLI renders the
template locally and writes the artifact atomically.

This is the phase tracer. Every layer the phase will modify is touched here — proto
contract, server streaming handler, client template package, CLI command, shared
atomic-write helper — with exactly one path wired through. Every later plan expands
outward from this proven slice.

Purpose: D-01 (client-side execution) and D-03 (struct top-level value) are one-way
decisions. Proving them end to end on the first commit means an architectural
dead-end costs one plan, not ten.
Output: `WatchHosts` RPC, `internal/server/watch.go`, `internal/client/template`,
`router-hosts render` command, `internal/atomicfile`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-CONTEXT.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-RESEARCH.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-PATTERNS.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-VALIDATION.md
@CLAUDE.md
</context>

## Source Coverage Audit (phase-wide)

Every item from all four source artifacts, mapped to the plan that delivers it.
No item is MISSING.

| Source | Item | Plan | Status |
|--------|------|------|--------|
| GOAL | Consumer defines its own output format; one server feeds N consumers | 01, 02, 07 | COVERED |
| GOAL crit 1 | Caller supplies template, receives rendered host data, no upstream code change | 01 | COVERED |
| GOAL crit 2 | Field set documented and versioned; undefined key fails loudly | 01, 02 | COVERED |
| GOAL crit 3 | Render/write failure leaves prior artifact byte-identical; no partial reads | 01, 07 | COVERED |
| GOAL crit 4 | Sink reflects mutation without intervention; recovers after interruption | 06, 07 | COVERED |
| GOAL crit 5 | Server yields lazily; client refuses unbounded response | 03, 04 | COVERED |
| GOAL crit 6 | Existing `unbound_conf_path` / `ExportHosts` format behavior unchanged | 01, 04, 08 | COVERED |
| REQ | TMPL-01 template-rendered output, no code change | 01 | COVERED |
| REQ | TMPL-02 documented + versioned field set | 01, 02 | COVERED |
| REQ | TMPL-03 undefined key fails loudly, no partial artifact | 01, 02, 07 | COVERED |
| REQ | TMPL-04 atomic write-and-rename | 01 | COVERED |
| REQ | TMPL-05 sink mode current + recovers | 05, 06, 07 | COVERED |
| REQ | TMPL-06 lazy streaming | 01, 04, 06 | COVERED (scope stated below) |
| REQ | TMPL-07 bounded client collection | 03 | COVERED |
| RESEARCH | `ImportHosts` is a false-friend; Watch needs a real goroutine pair | 06 | COVERED |
| RESEARCH | No keepalive configured anywhere; server default ping is 2h | 04 | COVERED |
| RESEARCH | TMPL-06 storage-layer laziness is not achievable without a new projection method | 04 (limitation recorded) | COVERED |
| RESEARCH | `missingkey=error` is defense-in-depth only, struct typing is the mechanism | 01 | COVERED |
| RESEARCH | `{{define "contract_version"}}` + `Template.Lookup()` for D-05 | 02 | COVERED |
| RESEARCH | `RegisterAggregateEventGauges` is the per-CN gauge precedent | 05 | COVERED |
| RESEARCH | `atomicWriteFile` must relocate; all call sites updated, tests stay green | 01 | COVERED |
| RESEARCH | Pitfall 3: `Comment`/`Tags` delivered raw; escaping is the template author's job | 02 (documented) | COVERED |
| CONTEXT | D-01 client-side template execution (one-way) | 01 | COVERED |
| CONTEXT | D-02 one renderer in one binary | 01 | COVERED |
| CONTEXT | D-03 struct top-level value with metadata (one-way) | 01 | COVERED |
| CONTEXT | D-04 per-entry field set | 01 | COVERED |
| CONTEXT | D-05 template declares contract version, CLI refuses on mismatch (costly) | 02 | COVERED |
| CONTEXT | D-06 full snapshot, coalesced | 06 | COVERED |
| CONTEXT | D-07 bidirectional watch RPC | 01 (shape), 06 (concurrency) | COVERED |
| CONTEXT | D-08 client-initiated throughout | 01, 06, 07 | COVERED |
| CONTEXT | D-09 sink health upstream on stream, exposed via server OTel keyed by CN | 05, 06 | COVERED |
| CONTEXT | D-10 last-seen survives stream close, lost on restart | 05 | COVERED |
| CONTEXT | D-11 local sidecar status file, written atomically | 07 | COVERED |
| CONTEXT | D-12 any failure leaves prior artifact byte-identical | 01, 07 | COVERED |
| CONTEXT | D-13 metric cardinality bounded by mTLS CN | 05 | COVERED |
| CONTEXT | D-14 configurable collection cap with safe default, fail loud | 03 | COVERED |
| CONTEXT | D-15 full collection implied by `.Count`; cap is what bounds it | 03 | COVERED |
| CONTEXT | D-16 post-write exec hook on the CLI | 07 | COVERED |

**TMPL-06 scope, stated explicitly (not silently narrowed):** this phase delivers
lazy streaming at the *transport and serialization* layer — one `HostEntry` per
`stream.Send` on `WatchHosts`, plus chunked sends on `ExportHosts` — which bounds
per-message wire memory and gives the client gRPC flow-control backpressure. It does
**not** make `store.ListAll()` a cursor read: that is an event-sourced fold over the
whole aggregate set (`internal/storage/sqlite/projection.go:19-45`) with no
paginated read path in `storage.HostProjection`. Adding one is a storage-layer
project outside this phase's blast radius; the limitation is recorded in plan 04's
`must_haves` and in the docs written by plan 02.

**Deferred Ideas from CONTEXT.md:** none — the discussion stayed in scope, so no plan
implements a deferred item.

## Artifacts this phase produces (plan 01 share)

Symbols created by this plan — newly created, so excluded from drift verification:

- proto: `WatchHosts` RPC, `WatchHostsRequest`, `WatchHostsResponse`, `SinkStatus`,
  `SnapshotComplete` messages (package `router_hosts.v1`)
- Go (generated): `HostsServiceClient.WatchHosts`, `HostsService_WatchHostsClient`,
  `grpc.BidiStreamingServer[hostsv1.WatchHostsRequest, hostsv1.WatchHostsResponse]`
- `internal/server/watch.go`: `(*HostsServiceImpl).WatchHosts`,
  `TemplateContractVersion` constant
- `internal/client/template`: `Data`, `Entry`, `Parse`, `Render`, `FromProto`
- `internal/client/commands/render.go`: `newRenderCmd`
- `internal/atomicfile`: `Write`
- CLI: `router-hosts render` command; `--template`, `--out` flags
- New file paths: `internal/atomicfile/atomicfile.go`, `internal/atomicfile/atomicfile_test.go`,
  `internal/server/watch.go`, `internal/server/watch_test.go`,
  `internal/client/template/template.go`, `internal/client/template/template_test.go`,
  `internal/client/commands/render.go`, `internal/client/commands/render_test.go`

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the two one-way architecture locks (D-01, D-03)</name>
  <read_first>
- .planning/phases/01-consumer-rendered-output-templates-sink/01-CONTEXT.md (sections "Execution site" and "Template data contract")
- .planning/phases/01-consumer-rendered-output-templates-sink/01-RESEARCH.md (Architectural Responsibility Map)
  </read_first>
  <decision>Lock in D-01 (templates execute client-side) and D-03 (the template's top-level value is a struct with .Entries plus metadata, not a bare slice) before any code implements them.</decision>
  <context>
Both decisions are rated one-way in CONTEXT.md.

D-01: moving execution server-side later means adding a template-execution
sandbox (timeout, output cap, function allowlist) to a single-writer DNS control
plane, and every consumer that came to rely on local-only rendering would need
its trust assumptions revisited.

D-03: with a bare slice, `.` is permanently bound to the entry list, so adding
any metadata field later silently changes what every existing consumer template
means. The struct is the only forward-compatible shape.

Task 2 implements both simultaneously. There is no cheap reversal after it lands.
  </context>
  <options>
<option id="proceed-as-locked">
  <name>Proceed exactly as locked in CONTEXT.md</name>
  <pros>Server never executes caller-supplied template text (largest threat removed by design). Metadata fields (.Count, .GeneratedAt, .ContractVersion) can grow without breaking existing templates.</pros>
  <cons>A consumer must run the router-hosts CLI locally (accepted in D-02, and sink mode already implies a long-lived local process).</cons>
</option>
<option id="revisit">
  <name>Revisit one or both before implementation</name>
  <pros>Last cheap moment to change direction.</pros>
  <cons>Invalidates 01-RESEARCH.md's Architectural Responsibility Map and 01-PATTERNS.md's file classification; the phase would need re-planning.</cons>
</option>
  </options>
  <acceptance_criteria>
- The operator has explicitly selected `proceed-as-locked` or `revisit`.
- If `revisit` is selected, execution halts and the phase returns to `/gsd-discuss-phase 1` before Task 2 starts.
  </acceptance_criteria>
  <resume-signal>Select: proceed-as-locked, or revisit</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end "render host data through my own template" — one path only</name>
  <files>proto/router_hosts/v1/hosts.proto, api/v1/router_hosts/v1/hosts.pb.go, api/v1/router_hosts/v1/hosts_grpc.pb.go, internal/server/watch.go, internal/server/watch_test.go, internal/client/template/template.go, internal/client/template/template_test.go, internal/client/commands/render.go, internal/client/commands/render_test.go, internal/client/commands/root.go</files>
  <read_first>
- proto/router_hosts/v1/hosts.proto (lines 1-60 for HostEntry, 260-275 for ExportHosts messages, 475-533 for the RPC block and its section-comment style)
- internal/server/service.go (lines 172-205 for mapError and domainToProto, 319-333 for the ListHosts per-entry send idiom, 441-460 for the BidiStreamingServer type signature)
- internal/server/service_test.go (lines 33-77 for newServiceTestEnv, the bufconn harness)
- internal/client/commands/host.go (lines 30-89 for Cobra construction, 328-359 for commandContext and collectHostStream)
- internal/client/commands/importexport.go (lines 148-231 for the export command's --output handling)
- internal/client/commands/root.go (lines 43-73 for NewRootCmd and subcommand registration)
- internal/client/commands/testhelper_test.go (setupCmdTest — the client-side bufconn harness)
- internal/server/hostsfile.go (lines 43-89 for FormatHostsFile, the generator formatting shape)
- .planning/phases/01-consumer-rendered-output-templates-sink/01-RESEARCH.md (Code Examples: contract-version block, missingkey semantics, per-entry streaming precedent)
  </read_first>
  <precondition>The `buf` CLI is installed and on PATH (`buf --version` exits 0), because `task proto:generate` regenerates the Go stubs this task compiles against.</precondition>
  <reversibility rating="one-way">Implements D-01 and D-03. Client-side execution and the struct-shaped top-level value both become a consumer-visible compatibility surface the moment a template is written against them.</reversibility>
  <behavior>
- Given a store holding one entry (192.168.1.10 / server.local, alias srv.local, tag web, comment "test"), a template body of `{{range .Entries}}{{.IPAddress}} {{.Hostname}}{{"\n"}}{{end}}` renders exactly `192.168.1.10 server.local` followed by a newline.
- Given the same store, a template body referencing `.Entries` and `.Count` renders the count 1.
- Given a template body referencing a field name that is not on Entry, Render returns a non-nil error and returns no rendered bytes.
- Given a `WatchHosts` request with follow unset, the server sends one response per entry followed by exactly one response carrying the snapshot terminator, then completes the stream.
- Given a `WatchHosts` request with follow set true, the server returns a gRPC Unimplemented status (filled in by plan 06).
  </behavior>
  <action>
Wire ONE entry point through every layer, end to end. No follow mode, no status
reporting, no file output, no contract-version enforcement, no collection cap —
those are expansion tasks in later plans. Real error handling on this one path.

1. `proto/router_hosts/v1/hosts.proto` — additive only; do not renumber or remove
any existing field or method. Add a new section comment `// Consumer-rendered
output (sink streaming)` after the `ExportHosts` rpc line and declare
`rpc WatchHosts(stream WatchHostsRequest) returns (stream WatchHostsResponse);`
inside the existing `HostsService` service block. Add four messages after
`ExportHostsResponse`, following this file's terse per-field `//` comment style:
`WatchHostsRequest` with `bool follow = 1;` and `optional SinkStatus status = 2;`;
`SinkStatus` with `int64 last_success_unix = 1;`, `int32 consecutive_failures = 2;`,
`string contract_version = 3;`; `SnapshotComplete` with `int32 count = 1;`,
`google.protobuf.Timestamp generated_at = 2;`, `string contract_version = 3;`;
`WatchHostsResponse` with a `oneof payload` containing `HostEntry entry = 1;` and
`SnapshotComplete complete = 2;`. Then run `task proto:generate` and `task fmt` —
never hand-edit anything under `api/v1/`.

2. `internal/server/watch.go` (new) — declare the exported constant
`TemplateContractVersion = "1"` with a doc comment stating it is the version of
the template data contract this server serves and that it changes only when a
field is renamed or removed from the contract. Implement
`func (s *HostsServiceImpl) WatchHosts(stream grpc.BidiStreamingServer[hostsv1.WatchHostsRequest, hostsv1.WatchHostsResponse]) error`.
Call `stream.Recv()` once to read the opening request; treat `io.EOF` on that
first Recv as a client that hung up and return nil. If the request's `GetFollow()`
is true, return `status.Error(codes.Unimplemented, "follow mode is not implemented yet")`
— plan 06 replaces this branch with the concurrent goroutine pair. Otherwise call
`s.store.ListAll(stream.Context())`, converting a storage error through the
existing `mapError`. Send one `WatchHostsResponse` per entry carrying
`domainToProto(&entries[i])` in the `entry` oneof arm — mirror `ListHosts` at
`service.go:319-333`, returning the raw send error unwrapped. After the loop send
one final `WatchHostsResponse` carrying `SnapshotComplete` with `Count` set to
`int32(len(entries))`, `GeneratedAt` set to `timestamppb.New(time.Now().UTC())`,
and `ContractVersion` set to `TemplateContractVersion`. Return nil.

3. `internal/client/template/template.go` (new, package `template`) — define
`type Entry struct` with exactly five exported fields: `IPAddress string`,
`Hostname string`, `Aliases []string`, `Tags []string`, `Comment string` (D-04).
Define `type Data struct` with `Entries []Entry`, `Count int`,
`GeneratedAt time.Time`, `ContractVersion string` (D-03). Every field on both
types must be a named struct field holding a scalar or a slice of scalars —
keyed-collection types are forbidden in this contract because `missingkey=error`
is the only guard for them and 01-RESEARCH.md Pitfall 1 shows it is not the
mechanism doing the real work. Provide
`func Parse(name, src string) (*texttemplate.Template, error)` returning
`texttemplate.New(name).Option("missingkey=error").Parse(src)` with the parse
error wrapped via `oops.Wrapf(err, "parse template")` (import `text/template`
under an alias so the package's own name does not shadow it). Provide
`func Render(tmpl *texttemplate.Template, data Data) ([]byte, error)` that
executes into a local `bytes.Buffer` and returns `buf.Bytes()` on success or
`nil` plus `oops.Wrapf(err, "execute template")` on failure — never write to a
destination file from inside Execute (D-12; 01-RESEARCH.md Anti-Patterns).
Provide `func FromProto(entries []*hostsv1.HostEntry, complete *hostsv1.SnapshotComplete) Data`
mapping `GetIpAddress`, `GetHostname`, `GetAliases`, `GetTags` and `GetComment`
(absent comment becomes the empty string), and taking `Count`, `GeneratedAt`
(via `complete.GetGeneratedAt().AsTime()`) and `ContractVersion` from the
terminator message the server sent.

4. `internal/client/commands/render.go` (new) — `func newRenderCmd() *cobra.Command`
with `Use: "render"`, a short description naming caller-supplied templates, a
required `--template` string flag (path to the template file) and `RunE` that:
reads the template file with `os.ReadFile` and parses it via `template.Parse`
BEFORE opening any connection, so a malformed template fails without touching the
server; builds a client via `newClientFromFlags()` with the same
`defer c.Close()` plus `slog.Warn` idiom used in `host.go`; uses
`commandContext()` for the 30s one-shot deadline; opens
`c.Hosts.WatchHosts(ctx)`, sends one `WatchHostsRequest` with follow left false,
calls `stream.CloseSend()`, then drains `stream.Recv()` accumulating entries from
the `entry` arm and capturing the `complete` arm, breaking on `io.EOF` via
`errors.Is`; returns `oops.Errorf` naming the missing terminator if the stream
ended without a `SnapshotComplete`; builds `template.FromProto(...)`, calls
`template.Render(...)`, and writes the rendered bytes to `cmd.OutOrStdout()`.
Register the command in `NewRootCmd` in `root.go` with
`root.AddCommand(newRenderCmd())` placed after the existing `newServeCmd()` line.

5. Tests. `internal/client/template/template_test.go`: table-driven tests named
`TestTemplateRender_HappyPath`, `TestTemplateRender_CountAndGeneratedAt`, and
`TestTemplateRender_UndefinedFieldFails` using testify `require`/`assert`.
`internal/server/watch_test.go`: `TestService_WatchHosts_OneShotSnapshot` built on
`newServiceTestEnv(t)`, asserting the response sequence is N entry messages then
exactly one terminator whose count equals N and whose contract version equals
`TemplateContractVersion`; and `TestService_WatchHosts_FollowUnimplemented`
asserting `codes.Unimplemented`. `internal/client/commands/render_test.go`:
`TestRender_TemplateEndToEnd` using `setupCmdTest(t)` — add a host through the
bufconn-backed client, write a template into `t.TempDir()`, execute the root
command with `render --template <path>`, and assert the captured stdout equals
the expected rendered text. Never write outside `t.TempDir()`.

Commit as `feat(proto): add WatchHosts streaming RPC` for step 1 and
`feat(client): render host data through caller templates` for steps 2-5.
  </action>
  <verify>
<automated>task proto:generate &amp;&amp; task lint &amp;&amp; task test -- -run 'TestTemplateRender_' ./internal/client/template/ &amp;&amp; task test -- -run 'TestService_WatchHosts_' ./internal/server/ &amp;&amp; task test -- -run 'TestRender_' ./internal/client/commands/</automated>
  </verify>
  <acceptance_criteria>

- `task proto:generate` leaves no uncommitted diff under `api/v1/` when run twice in a row.
- `rg -n 'rpc WatchHosts' proto/router_hosts/v1/hosts.proto` returns exactly one line.
- The proto change is purely additive: `git diff -U0 proto/router_hosts/v1/hosts.proto | rg '^-[^-]' | wc -l` returns 0, meaning no pre-existing line was removed or altered and therefore no field number or method signature changed.
- `task test -- -run 'TestTemplateRender_' -v ./internal/client/template/` reports 3 PASS lines and 0 FAIL.
- `task test -- -run 'TestService_WatchHosts_' -v ./internal/server/` reports 2 PASS lines and 0 FAIL.
- `task test -- -run 'TestRender_' -v ./internal/client/commands/` reports at least 1 PASS line and 0 FAIL.
- `rg -c 'map\[' internal/client/template/template.go` returns 0 (the contract is entirely struct-typed, per D-03/D-04).
- `rg -n 'Option\("missingkey=error"\)' internal/client/template/template.go` returns exactly one line.
- `task test ./internal/server/ ./internal/client/...` is green (no existing test regressed).
- `task lint` exits 0 with no new `//nolint` directive introduced (`git diff` contains no added line matching `nolint`).
  </acceptance_criteria>
  <done>A caller with a template file and mTLS credentials runs `router-hosts render --template ./x.tmpl` against a live server and gets their own format on stdout. The single happy path works end to end and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Relocate the atomic-write helper and give render an atomic --out</name>
  <files>internal/atomicfile/atomicfile.go, internal/atomicfile/atomicfile_test.go, internal/server/hostsfile.go, internal/server/hostsfile_test.go, internal/server/unboundconf.go, internal/server/dnsmasqconf.go, internal/client/commands/render.go, internal/client/commands/render_test.go</files>
  <read_first>
- internal/server/hostsfile.go (lines 131-164 — the function being moved; lines 28-41 — the caller)
- internal/server/hostsfile_test.go (lines 181-227 — the four tests being moved)
- internal/server/unboundconf.go (line 59 — the second call site)
- internal/server/dnsmasqconf.go (line 51 — the third call site)
- internal/server/hooks_wiring_test.go (line 99 — a test whose comment references the helper's failure mode)
- internal/client/commands/render.go (written in Task 2)
- .planning/phases/01-consumer-rendered-output-templates-sink/01-PATTERNS.md (section on internal/atomicfile — the verbatim relocation instructions)
  </read_first>
  <behavior>
- `atomicfile.Write` on a fresh path creates the file with exactly the given bytes and leaves no temp file beside it.
- `atomicfile.Write` over an existing file replaces its contents entirely.
- `atomicfile.Write` to a path whose parent directory does not exist returns an error mentioning temp-file creation and creates nothing.
- `render --out <path>` with a template that renders successfully writes the rendered bytes to that path.
- `render --out <path>` with a template whose execution fails leaves a pre-existing file at that path byte-identical and returns a non-zero error.
  </behavior>
  <action>
Move, do not copy. There must be exactly one write-and-rename implementation in
the tree when this task is done.

1. Create `internal/atomicfile/atomicfile.go`, package `atomicfile`, with a
package doc comment stating it is the shared write-and-rename helper used by
server output generators and by client-side artifact writes. Move the body of
`atomicWriteFile` from `internal/server/hostsfile.go:133-164` verbatim, changing
only the name and signature to `func Write(path string, content []byte) error`
and swapping `f.WriteString(content)` for `f.Write(content)`. Keep every
`oops.Wrapf` message string exactly as it is today ("create temp file",
"write file", "fsync file", "close file", "rename file") so existing error
assertions keep matching. Keep the `os.CreateTemp(filepath.Dir(path), filepath.Base(path)+".tmp.*")`
call unchanged so the temp file lands on the same filesystem as the destination
and the rename stays atomic.

2. Delete `atomicWriteFile` from `internal/server/hostsfile.go` and update all
three call sites to `atomicfile.Write(g.path, []byte(content))`:
`hostsfile.go:37`, `unboundconf.go:59`, `dnsmasqconf.go:51`. Add the
`github.com/fzymgc-house/router-hosts/internal/atomicfile` import to each file
and drop now-unused imports (`os`, `path/filepath`) from `hostsfile.go` if the
compiler reports them unused.

3. Move the four tests at `internal/server/hostsfile_test.go:181-227` into
`internal/atomicfile/atomicfile_test.go`, package `atomicfile`, keeping their
names (`TestAtomicWrite_NewFile`, `TestAtomicWrite_OverwritesExisting`,
`TestAtomicWrite_CleansUpTmp`, `TestAtomicWrite_InvalidPath`) and bodies, with
the calls rewritten to `Write(path, []byte("test content\n"))` and friends.
Delete them from `hostsfile_test.go` and remove imports that go unused there.
`internal/acme/acme.go` has its own unrelated `atomicWriteFile` with a permissions
argument — leave it entirely alone.

4. `internal/client/commands/render.go` — add an `--out` string flag documented
as the artifact path (empty means stdout). In `RunE`, after `template.Render`
succeeds: when `--out` is empty write to `cmd.OutOrStdout()` as before; otherwise
call `atomicfile.Write(outPath, rendered)` and wrap any error with
`oops.Wrapf(err, "writing artifact %s", outPath)`. Because rendering completes
into a buffer before any write is attempted, a render failure returns before
`atomicfile.Write` is ever reached — that is what keeps a previously written
artifact byte-identical (D-12). Do not add any `os.Create`, `os.OpenFile`, or
truncating write on the destination path.

5. `internal/client/commands/render_test.go` — add `TestRender_WritesArtifactToOut`
(renders to a `t.TempDir()` path and asserts the file bytes) and
`TestRender_FailurePreservesArtifact` (pre-writes a known artifact, runs `render`
with a template referencing a field that does not exist on Entry, asserts the
command returns an error AND that the artifact bytes are unchanged from the
pre-written value).

Commit as `refactor(server): extract shared atomic file writer` for steps 1-3 and
`feat(client): write rendered artifact atomically` for steps 4-5.
  </action>
  <verify>
<automated>task test -- -run 'TestAtomicWrite_' ./internal/atomicfile/ &amp;&amp; task test ./internal/server/ &amp;&amp; task test -- -run 'TestRender_' ./internal/client/commands/ &amp;&amp; task lint</automated>
  </verify>
  <acceptance_criteria>

- `task test -- -run 'TestAtomicWrite_' -v ./internal/atomicfile/` reports 4 PASS lines and 0 FAIL.
- `rg -n 'func atomicWriteFile' internal/server/` returns no matches (the server copy is gone; `internal/acme` keeps its own unrelated helper).
- `rg -c 'atomicfile\.Write' internal/server/hostsfile.go internal/server/unboundconf.go internal/server/dnsmasqconf.go` returns 1 for each of the three files.
- `task test ./internal/server/` is green — the hosts, dnsmasq and unbound generator tests all still pass unmodified, which is the regression gate for phase success criterion 6.
- `task test -- -run 'TestRender_' -v ./internal/client/commands/` reports at least 3 PASS lines and 0 FAIL.
- `rg -n 'os\.Create\(|os\.OpenFile\(|os\.WriteFile\(' internal/client/commands/render.go` returns no matches — the destination path is only ever touched through the shared helper.
- `task lint` exits 0.
  </acceptance_criteria>
  <done>One write-and-rename implementation serves both server generators and the client render path; `render --out` produces a complete artifact or leaves the previous one untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client CLI to server (mTLS gRPC) | Every `WatchHosts` caller is CA-verified (`tls.RequireAndVerifyClientCert`, `internal/server/server.go:207`); the request body itself is untrusted input |
| server to consumer artifact file | Rendered bytes cross into a file that a resolver daemon will read and act on |
| operator-supplied template file to `text/template` | Template source is read from a local path and executed in the CLI process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | server-side execution of caller-supplied template text | high | mitigate | Mitigated by design: D-01 puts execution in the consumer's own CLI process, so no template sandbox (timeout, output cap, function allowlist) is ever needed in the single-writer control plane. The server sends structured `HostEntry` messages only and never parses template text. Confirmed by the Task 1 checkpoint. |
| T-1-02 | Tampering | `--out` artifact path (traversal / symlink swap) | medium | mitigate | The write goes through `atomicfile.Write`, which creates its temp file in `filepath.Dir(path)` and `os.Rename`s over the destination — a rename replaces a symlink at the destination rather than following it, so an attacker-planted symlink cannot redirect the write. The CLI runs with the consumer operator's own privileges and the path is operator-supplied, so no additional confinement is added; documented in plan 02's contract doc. |
| T-1-03 | Tampering | partially written artifact observed by a concurrent resolver reload | high | mitigate | Render completes into a `bytes.Buffer` before any file syscall (D-12), then `atomicfile.Write` does CreateTemp/Write/Sync/Close/Rename. `TestRender_FailurePreservesArtifact` proves the byte-identical guarantee; `TestAtomicWrite_*` prove the rename path. |
| T-1-04 | Information Disclosure | rendered artifact file permissions | low | accept | `os.CreateTemp` creates the temp file mode 0600 and the rename preserves it, which is at least as restrictive as the pre-existing server generator behavior this helper was extracted from. No change in posture; not widened by this phase. |
| T-1-05 | Denial of Service | unbounded entry accumulation in the `render` client loop | high | mitigate | Deferred to plan 03 by design (TMPL-07 / D-14) — plan 03 is a hard dependency of this phase's completion and applies the cap to the render drain loop as well as the pre-existing collectors. Until plan 03 lands, `render` is exposed to a malicious server exactly as much as today's `host list` is. |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable: 01-RESEARCH.md's Package Legitimacy Audit records zero new external packages — every import is stdlib or an already-pinned `go.mod` dependency. No package-manager install task exists in this plan. |

</threat_model>

<verification>
- `task proto:generate` is idempotent (running it twice leaves the working tree clean).
- `task lint` and `task fmt` are clean.
- `task test ./...` is green — in particular every pre-existing `internal/server` generator test passes unmodified after the atomic-write relocation.
- The end-to-end tracer test drives CLI to gRPC to store and back, not a mocked seam.
</verification>

<success_criteria>

- `router-hosts render --template <file>` prints host data in the caller's own format.
- `router-hosts render --template <file> --out <path>` writes that format atomically.
- A template referencing a nonexistent field returns an error and writes nothing.
- `internal/atomicfile.Write` is the only write-and-rename helper reachable from `internal/server`.
- `WatchHosts` streams one message per entry plus one terminator carrying count, generation time, and contract version.
</success_criteria>

<output>
Create `.planning/phases/01-consumer-rendered-output-templates-sink/01-01-SUMMARY.md` when done
</output>
