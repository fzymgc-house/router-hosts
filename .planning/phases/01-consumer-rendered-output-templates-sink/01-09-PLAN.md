---
phase: 01-consumer-rendered-output-templates-sink
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/eventid/eventid.go
  - internal/eventid/eventid_test.go
  - internal/server/commands.go
  - internal/server/commands_test.go
  - internal/storage/storage.go
  - internal/storage/sqlite/sqlite.go
  - internal/storage/sqlite/sqlite_test.go
  - internal/storage/sqlite/eventstore.go
  - internal/storage/sqlite/compaction_monotonic_test.go
  - internal/storage/sqlite/eventid_guard_test.go
  - internal/storage/storagetest/suite.go
autonomous: true
requirements:
  - TMPL-08
estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No event is committed to the log carrying an ID that sorts at or below MAX(event_id) as of that commit: every insert re-reads the maximum inside the same write transaction and re-mints strictly above it when the proposed ID does not already sort greater, so mint order does not have to equal commit order (TMPL-08, D-20, review H2)"
    - "The in-transaction check lives in insertEvent, the one INSERT helper that AppendEvent, AppendEvents, AppendEventsBatch and CompactAggregate all share, so a caller-constructed envelope reaching the storage boundary cannot bypass it (review H4)"
    - "The event-ID generator's floor is seeded from LatestEventID when a store is initialized, so the first ID minted after a process restart sorts strictly above the persisted maximum even when it lands inside the same millisecond as the last pre-restart append (review H3)"
    - "Two concurrent direct storage writers whose commits land in the reverse of their mint order each leave MAX(event_id) strictly greater than it was before that commit, so neither mutation is invisible to a change-ID consumer (review H2, M7)"
    - "Compacting an aggregate advances MAX(event_id) rather than leaving it pinned or moving it backward, which is the premise the change ID rests on (D-20, D-22, review round-2 H2)"
    - "LatestEventID returns the lexicographically greatest event_id in the log, which under the in-transaction guard is the most recently committed event (TMPL-08, D-18)"
    - "An empty event log yields the zero-ULID sentinel rather than an error or an empty string (D-22)"
    - "The generator is safe under concurrent use — this is goroutine safety of the entropy reader and the floor, a data-race property, and is deliberately NOT the ordering guarantee, which the in-transaction guard supplies instead"
    - "LatestEventID on an empty log returns the zero ULID with a nil error; after exactly one appended event it returns that event's ID"
    - "Two events minted inside the same millisecond receive strictly increasing IDs, so MAX(event_id) advances between them rather than tying"
    - "An empty store yields storage.ZeroChangeID on the wire rather than an empty string or an error"
    - "The change ID is a 26-character Crockford base-32 ULID string compared by exact string equality; no numeric conversion, truncation, or lexical prefix comparison is performed anywhere"
    - "The remaining residual is named rather than claimed away: the guard is implemented per backend, so any future EventStore implementation must satisfy the same compliance case or it reintroduces the hazard inside its own append path"
    - "The storage contract states that a proposed EventID may be replaced by the store, so no caller may assume the ID it supplied is the ID persisted"
  prohibitions:
    - statement: "MAX(event_id) must not be replaced by a database-owned monotonic sequence or a separate revision column. D-18 fixes the change ID as the ULID of the newest event and D-19 fixes it as a value identifying state rather than transmission; a sequence would require a schema migration, a proto change from a ULID string to a numeric revision, and an amendment to two locked decisions. The ordering hazard is closed inside the append transaction instead, which is what makes mint order irrelevant without changing what the change ID IS."
      status: unverified
      verification: flagged
  artifacts:
    - internal/eventid/eventid.go
    - internal/storage/storage.go
    - internal/storage/sqlite/eventstore.go
    - internal/storage/sqlite/sqlite.go
    - internal/storage/storagetest/suite.go
  key_links:
    - "internal/eventid.New is the single event-ID minting function and holds a monotonic floor; CommandHandler.newID, CompactAggregate's seed envelope and the in-transaction guard all mint through it"
    - "internal/storage/sqlite.insertEvent is the single INSERT site every append path funnels through, which is why the in-transaction ordering guard placed there covers AppendEvent, AppendEvents, AppendEventsBatch and CompactAggregate at once"
    - "sqlite.Storage.Initialize calls eventid.Seed(LatestEventID) after migrations, which is what makes the process generator monotonic against the PERSISTED log rather than only against itself"
    - "storage.EventStore.LatestEventID reads MAX(event_id) over the events table, whose event_id column is TEXT PRIMARY KEY, so the read is an indexed seek"
    - "storage.ZeroChangeID is the one spelling of the empty-store sentinel, shared by the sqlite implementation, the compliance suite and the server"
---

# Phase 1 Plan 09 — Change-ID Storage Foundation: Commit-Ordered Event IDs and `LatestEventID`

<objective>
Establish the storage-layer premise TMPL-08 rests on, before any plan derives a change
ID from it: an event log in which no commit can land an ID at or below the log's
current maximum, and a `LatestEventID` read that returns that maximum.

Purpose: `MAX(event_id)` is a valid change ID **only** if no commit can insert an ID
that sorts below an ID already in the log. That is currently false.
Output: `internal/eventid` (a monotonic generator with a persisted-log floor), an
in-transaction ordering guard at the single INSERT site,
`storage.EventStore.LatestEventID`, `storage.ZeroChangeID`, and deterministic
regression tests for compaction, restart, a caller-supplied lower ID, and
reverse-order concurrent commits.

**Created 2026-08-01 in response to cross-AI review round 2** (`01-REVIEWS.md` H2).
Codex checked the claim the round-1 revision made and found it false in the repo
today: `internal/storage/sqlite/eventstore.go:258` mints the `HostCompacted`
replacement seed with a bare `ulid.Make()` inside `CompactAggregate`, bypassing
`CommandHandler.newID` (`internal/server/commands.go:60`) and the shared
`*ulid.MonotonicEntropy` entirely.

The consequence is precisely the failure D-20 exists to prevent. A same-millisecond
bare ULID can sort **below** the maximum it replaces, so a compaction can leave
`MAX(event_id)` pinned or move it backward — the change ID fails to advance across a
real state change and a deduping consumer silently keeps serving a stale zone. D-22
anticipated compaction moving the ID *forward* and accepted one redundant render; it
did not anticipate it moving *backward*.

## Revised 2026-08-01 after cross-AI review round 3 (`01-REVIEWS.md` H2, H3, H4, M4, M7, L3)

**Round 3 adjudicated the round-2 remedy insufficient, and the adjudication is
correct.** Routing both known mint sites through one process-wide monotonic generator
does not establish the invariant this plan asserted. Three separate gaps, all closed
here by one mechanism.

**H2 — mint order is not commit order, and this is the serious one.** Event IDs are
minted **outside** the write-queue lock in every command. `internal/server/commands.go:115`
(`id := h.newID()`) and `:119` (`newEnvelope`, which mints `EventID` at `:77`) both run
**before** `h.submitWrite(...)` at `:135`; the same shape repeats at `:201-268` before
`:284`, and `:345` before `:354`. Only the duplicate check and the store write execute
inside the queue. So: goroutine A mints ID_A; goroutine B mints ID_B greater than ID_A;
B wins the queue and commits, leaving the log maximum at ID_B; A then commits an ID
that sorts **below** an ID already in the log, and `MAX(event_id)` does not advance
across A's commit. A consumer holding ID_B computes the same change ID, the D-21 skip
fires, and A's mutation is never rendered. No restart and no clock anomaly is required
— ordinary concurrent writes reach it.

**H3 — the generator is process-local**, so an ID minted immediately after a restart,
inside the same millisecond as the last pre-restart append, can sort below it. The
round-2 plan documented this as accepted while *also* asserting an unqualified "no
append path can produce an ID that sorts below an ID already in the log". That
contradiction was the defect independent of any remedy.

**H4 — the storage boundary accepts caller-constructed envelopes.**
`AppendEvent`, `AppendEvents` and `AppendEventsBatch` all take a
`domain.EventEnvelope` the caller built (`internal/storage/storage.go:51`), so routing
two known production sites through a shared generator cannot make an interface-level
guarantee true.

**The user's decision, implemented here: seed the generator from `LatestEventID` at
storage startup, AND re-check the proposed event ID against `MAX(event_id)` inside the
append transaction.** The in-transaction check is the load-bearing half — it makes
mint order irrelevant, which no process-local generator can achieve — and it closes
H2, H3 and H4 together. `internal/storage/sqlite/eventstore.go:313` `insertEvent` is
the single INSERT helper that `AppendEvent` (`:21`), `AppendEvents` (`:55`),
`AppendEventsBatch` (`:93`) and `CompactAggregate` (`:210`) all funnel through, and
every one of them calls it inside a `sqlitex.ImmediateTransaction`, so one guard there
covers every append path including a caller-constructed envelope.

**The rejected alternative, recorded so it is not re-proposed:** a database-owned
monotonic sequence assigned in the append transaction. It is a stronger mechanism, but
D-18 fixes the change ID as *the ULID of the newest event* and the proto carries it as
a ULID string. Adopting a sequence means a schema migration, a wire-type change, and an
amendment to a locked decision. **D-18 and D-19 are unchanged by this plan, there is no
schema migration, and the wire type stays a ULID string.** The prohibition is recorded
in `must_haves.prohibitions`.

**The invariant this plan now claims is the one the mechanism actually delivers:** not
"every ID is minted monotonically" (unachievable across processes and arbitrary
callers) but "**no commit lands an ID at or below the log maximum**". Mint order is
allowed to be arbitrary; commit order is what the guard makes authoritative.

**A note on CONTEXT.md D-20.** D-20's *mandate* — any change to ID generation must
preserve monotonicity — is exactly what this plan implements. D-20's *rationale* text
contains a factual error about the current code (it says every append path mints
through `newID()`, which is false both for compaction at `eventstore.go:258` and for
the mint-versus-commit ordering above). **Do not repeat that claim anywhere in code
comments, doc comments, or the SUMMARY.** CONTEXT.md is not edited by this plan — it
records locked user decisions and the discrepancy is being surfaced to the user
separately.

**M4, M7, L3 — three smaller round-3 items.** `mu sync.Mutex // protects entropy`
(`commands.go:23`) exists solely to guard the field being removed and must go with it.
The compaction regression test is probabilistic and covers none of the newly-reachable
cases, so deterministic tests are added for restart seeding, an explicitly lower
caller-supplied ID, and two concurrent writers committing in reverse mint order. And
the executor records the **observed** RED failure count out of 50 in the SUMMARY rather
than "failed at least once", so a future reader can tell "fails about half the time as
predicted" from "fails for an unrelated reason".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-CONTEXT.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-RESEARCH.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-PATTERNS.md
@.planning/phases/01-consumer-rendered-output-templates-sink/01-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces (plan 09 share)

Symbols created by this plan — newly created, so excluded from drift verification:

- `internal/eventid`: package `eventid`; `New`, `Seed` and `NewAfter` functions;
  unexported `next` helper and the package-level `floor`/entropy state
- `internal/storage`: `EventStore.LatestEventID` interface method, `ZeroChangeID`
  constant
- `internal/storage/sqlite`: `(*Storage).LatestEventID`, unexported
  `selectLatestEventID` query helper, the in-transaction ordering guard inside
  `insertEvent`, and the `eventid.Seed` call at the end of `Initialize`
- `internal/storage/storagetest`: `EventStoreLatestEventID`,
  `EventStoreCompactionAdvancesLatestEventID` and
  `EventStoreAppendNeverLowersLatestEventID` compliance cases
- New file paths: `internal/eventid/eventid.go`, `internal/eventid/eventid_test.go`,
  `internal/storage/sqlite/compaction_monotonic_test.go`,
  `internal/storage/sqlite/eventid_guard_test.go`

## Flagged assumptions (spec-less probe fallback)

No `-SPEC.md` exists for this phase, so edge coverage was derived by the deterministic
edge probe. Every TMPL-08 edge the probe surfaced is authored into `must_haves.truths`
above. No TMPL-08 row was left unclassified, so this plan surfaces no flagged
assumption; the two unclassified rows in the report (TMPL-01, TMPL-03) are surfaced by
plans 01 and 02 respectively. The round-3 revision adds truths and one prohibition; it
removes none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One event-ID generator with a monotonic floor</name>
  <files>internal/eventid/eventid.go, internal/eventid/eventid_test.go, internal/server/commands.go, internal/server/commands_test.go, internal/storage/sqlite/eventstore.go, internal/storage/sqlite/compaction_monotonic_test.go</files>
  <read_first>
- internal/server/commands.go (lines 17-33 and 56-80 — the `entropy *ulid.MonotonicEntropy` field, the `mu sync.Mutex` beside it at :23 that guards nothing else, both constructors that build the entropy source, `newID()` at :56-61, and `newEnvelope` at :77 which is the single stamping site)
- internal/server/commands.go (lines 105-140, 195-290 and 340-360 — read these to see with your own eyes that `newID()`/`newEnvelope` run BEFORE `submitWrite`, which is why a process-local generator alone cannot order commits; Task 3 is what fixes that)
- internal/storage/sqlite/eventstore.go (lines 205-290 — the whole `CompactAggregate` body, especially the `EventEnvelope` literal at :252-262 whose `EventID: ulid.Make()` is the bug; note the surrounding `withConn` + `ImmediateTransaction` shape)
- internal/storage/sqlite/migrations/001_initial.sql (lines 1-13 — `event_id TEXT PRIMARY KEY`, which is why lexical ordering is the ordering that matters and why `MAX()` is an indexed seek)
- internal/server/commands_test.go (lines 240-260 and 515-545 — the existing envelope-construction test style; confirm none of them reads the `entropy` or `mu` fields before removing them)
- .planning/phases/01-consumer-rendered-output-templates-sink/01-CONTEXT.md (D-18, D-20 and D-22 in full — the change-ID premise, the monotonicity mandate, and the compaction acceptance. Note the caveat in this plan's objective about D-20's rationale text)
  </read_first>
  <behavior>
- `eventid.New()` called N times in a tight loop with no sleep returns N strictly increasing ULIDs, even though many of them share a millisecond.
- `eventid.New()` called concurrently from many goroutines returns ULIDs that are all distinct, and the run is clean under the race detector.
- After `eventid.Seed(u)` with a `u` far in the future, the next `eventid.New()` sorts strictly greater than `u`.
- `eventid.Seed(u)` with a `u` below the current floor does not lower the floor: the next `New()` still sorts strictly greater than everything previously returned.
- `eventid.NewAfter(u)` returns a ULID strictly greater than `u` for any `u`, including one whose entropy bytes are all `0xFF`.
- A `CommandHandler` built by either constructor stamps envelopes with IDs that are strictly increasing across consecutive appends.
- Compacting an aggregate whose events include the log's current maximum event ID leaves the log's maximum strictly greater than it was before the compaction.
- Compacting an aggregate that does not hold the log's maximum still leaves the maximum strictly greater than before, because the replacement seed is newly minted (D-22's accepted redundant render).
  </behavior>
  <action>
Create one minting function with a raisable floor, and make both existing mint sites
call it. There must be exactly one place in non-test code that constructs an event ID
when this task is done.

1. Create `internal/eventid/eventid.go`, package `eventid`, with a package doc
comment recording what the package exists for: every `EventEnvelope.EventID` in this
system is a ULID, and `MAX(event_id)` is used as the server's change ID (TMPL-08,
D-18), so an ID that sorts below an ID already in the log would make the change ID
fail to advance across a real state change — and a deduping consumer would then keep
serving a stale zone with no error anywhere.

State the division of responsibility plainly in that doc comment, because it is the
thing a later reader will get wrong: **this package makes minting monotonic; it does
not make commits ordered.** The ordering guarantee the change ID depends on is
enforced in the append transaction (`internal/storage/sqlite`), and this package is
what that guard mints through. Do not write a comment claiming that using this
package makes every append monotonic — it does not, and the previous revision of this
plan asserted exactly that and was wrong.

Declare package-level state under one `sync.Mutex`: an entropy source built from
`ulid.Monotonic(rand.Reader, 0)` (`crypto/rand`), matching what `CommandHandler`
builds today at `internal/server/commands.go:32` and `:43`, and a `floor ulid.ULID`
holding the greatest value this package has issued or been seeded with. The `inc`
argument 0 is meaningful: in the pinned `github.com/oklog/ulid/v2 v2.1.2` it defaults
the increment ceiling to `math.MaxUint32`, which is what makes successive IDs inside
one millisecond strictly increasing rather than random. Record that in a comment.

Expose three functions:

- `func New() ulid.ULID` — under the lock, mint
  `ulid.MustNew(ulid.Timestamp(time.Now()), <entropy>)`; if the result does not
  compare strictly greater than `floor`, replace it with `next(floor)`; store the
  result as the new `floor` and return it. The floor comparison is what makes a
  backwards wall-clock step and a same-millisecond restart harmless rather than
  merely rare.
- `func Seed(u ulid.ULID)` — under the lock, raise `floor` to `u` when `u` compares
  greater than the current floor, and otherwise do nothing. Document that it is
  monotonic and idempotent so a caller cannot lower the floor, and that its purpose
  is to make the process generator monotonic against a **persisted** log at startup.
- `func NewAfter(lower ulid.ULID) ulid.ULID` — `Seed(lower)` then `New()`, documented
  as the entry point for the in-transaction re-mint in Task 3 and guaranteed to
  return a value strictly greater than `lower`.

Add an unexported `func next(u ulid.ULID) ulid.ULID` returning the smallest ULID
strictly greater than `u`: copy `u`, increment the 80-bit entropy portion (bytes 6
through 15, big-endian) with carry; if it wraps to all zeros, increment the 48-bit
timestamp portion (bytes 0 through 5) with carry and leave the entropy at zero.
Document that carrying into the timestamp can produce an ID whose embedded
millisecond is a hair in the future, which is correct and preferable to a tie — the
timestamp is an ordering key here, not a claim about wall-clock time.

Prefer the library's own concurrency wrapper for the reader: run
`go doc github.com/oklog/ulid/v2 LockedMonotonicReader` first, and if that type exists
in the pinned version, wrap the monotonic reader in it. The package `sync.Mutex` is
required regardless, because the floor read-compare-write must be atomic with the
mint. Do not leave the reader unguarded either way — `*ulid.MonotonicEntropy` is not
goroutine-safe, and this package is now called from the command path, from
`CompactAggregate`, and from inside the storage append transaction.

2. `internal/server/commands.go` — delete the `entropy *ulid.MonotonicEntropy` field
from `CommandHandler`, delete the `mu sync.Mutex` field at `:23` whose comment says it
guards that entropy and which guards nothing else (review M4 — confirm by reading
every use of `h.mu` in the file before deleting), delete the
`entropy: ulid.Monotonic(rand.Reader, 0)` initialiser from both constructors, and make
`newID()` return `eventid.New()`. Keep `newID` as the method so `newEnvelope` and every
caller are unchanged. Add a one-line doc comment on `newID` pointing at
`internal/eventid` for why the generator is shared rather than per-handler. Drop the
now-unused `crypto/rand` and `sync` imports if the compiler reports them unused, and
check `time` rather than assuming — it is used elsewhere in that file.

3. `internal/storage/sqlite/eventstore.go` — in `CompactAggregate`, change the
`EventEnvelope` literal's `EventID: ulid.Make()` (line 258) to `EventID: eventid.New()`.
Add a comment at that line stating that the seed must be minted through the shared
generator because `MAX(event_id)` is the server's change ID: a bare ULID minted here
can sort below the maximum this compaction just deleted, which would make the change
ID fail to advance or move backward. Import
`github.com/fzymgc-house/router-hosts/internal/eventid`. Change nothing else in the
function — the delete-then-insert transaction, the `highWater` version handling and
the deadline rollback all stay exactly as they are.

Confirm there is no import cycle before writing: `internal/eventid` imports only
`crypto/rand`, `sync`, `time` and `github.com/oklog/ulid/v2`, so both `internal/server`
and `internal/storage/sqlite` can import it.

4. Tests. `internal/eventid/eventid_test.go`: `TestEventID_StrictlyIncreasing`
(at least 1000 consecutive `New()` calls with no sleep, asserting each compares
strictly greater than its predecessor with `ulid.ULID.Compare`, so many share a
millisecond and the assertion exercises monotonic entropy rather than the clock);
`TestEventID_Unique` (the same set has no duplicates);
`TestEventID_ConcurrentUse` (goroutines minting into a channel, joined with a
`sync.WaitGroup`, asserting all IDs are distinct — `task test` already runs `-race`,
so an unguarded reader or floor fails here);
`TestEventID_SeedRaisesFloor` (seed a ULID one hour in the future, assert the next
`New()` sorts strictly greater than it);
`TestEventID_SeedIgnoresLowerValue` (seed a ULID one hour in the past, assert the next
`New()` is still strictly greater than the previous `New()`); and
`TestEventID_NewAfterAlwaysGreater` (table-driven over a zero ULID, a normal ULID, and
one whose entropy bytes are all `0xFF` so the carry into the timestamp is exercised).

`internal/storage/sqlite/compaction_monotonic_test.go`: add
`TestCompactAggregate_AdvancesLatestEventID` in the style of the existing sqlite
tests. Seed an aggregate with several events, read the log maximum, compact that
aggregate, and assert the new maximum is strictly greater than the one recorded
before. Add `TestCompactAggregate_AdvancesWhenAggregateHeldTheMaximum`, seeding a
second aggregate first so the compacted aggregate demonstrably holds the global
maximum before compaction — that is the exact case a bare `ulid.Make()` breaks.
Before accepting either test, verify it goes RED against the pre-fix code: temporarily
restore `ulid.Make()` at the seed site, run the test in a loop
(`task test -- -count=50 -run TestCompactAggregate_Advances ./internal/storage/sqlite/`)
and **record how many of the 50 runs failed** (review L3), then revert. Two
same-millisecond ULIDs sort in a random order, so a single run of the broken code
passes about half the time; recording the observed count lets a future reader
distinguish "fails about half the time as predicted" from "fails for an unrelated
reason". A regression test never observed failing is indistinguishable from one that
cannot fail. Put the observed count in the SUMMARY.

Commit as `fix(storage): mint every event ID through one generator`.
  </action>
  <verify>
<automated>task test -- -run 'TestEventID_' ./internal/eventid/ &amp;&amp; task test -- -count=50 -run 'TestCompactAggregate_Advances' ./internal/storage/sqlite/ &amp;&amp; task test ./internal/server/ ./internal/storage/... &amp;&amp; task lint</automated>
  </verify>
  <acceptance_criteria>

- `task test -- -run 'TestEventID_' -v ./internal/eventid/` reports 6 PASS lines and 0 FAIL.
- `task test -- -count=50 -run 'TestCompactAggregate_Advances' ./internal/storage/sqlite/` is green — 50 repeated runs, because a single run of the broken code passes roughly half the time.
- The SUMMARY records the observed RED failure count out of 50 as a number, not as "failed at least once" (review L3).
- No non-test file constructs an event ID outside the shared generator: `rg -n 'EventID:\s*ulid\.Make\(\)' internal/ --glob '!**/*_test.go'` returns no matches. <!-- planner-discipline-allow: EventID: ulid.Make() --> Write the comment at the compaction seed site about the hazard in general terms ("a bare ULID minted here can sort below the maximum this compaction just deleted"); do not paste the old field-and-call sequence into a comment, or this gate fails on the comment.
- Every non-test match for `rg -n 'ulid\.Monotonic' internal/ --glob '!**/*_test.go'` is inside `internal/eventid/eventid.go` — no other package builds its own entropy source. Deliberately an "only in this file" check rather than an exact count of one, so the doc comment explaining the `inc` argument is free to name the constructor.
- `rg -n 'entropy' internal/server/commands.go` returns no matches. <!-- planner-discipline-allow: entropy --> The per-handler source is gone rather than shadowed by a second one; write `newID`'s doc comment as a pointer to `internal/eventid` without naming the removed field, or this gate fails on the comment.
- The orphaned mutex is gone with it (review M4): `rg -n 'sync\.Mutex' internal/server/commands.go` returns no matches, and `rg -n 'h\.mu' internal/server/commands.go` returns no matches. <!-- planner-discipline-allow: sync.Mutex, h.mu --> Do not leave a comment in `commands.go` explaining what was removed, or this gate fails on the comment; the removal is recorded here and in the SUMMARY instead.
- `rg -c 'eventid\.New\(\)' internal/server/commands.go internal/storage/sqlite/eventstore.go` returns at least 1 for each file — both former minting sites now route through the shared generator.
- The three entry points exist and are exported: `rg -n '^func (New|Seed|NewAfter)\(' internal/eventid/eventid.go` returns exactly 3 lines.
- The concurrency guard exists: `rg -n 'LockedMonotonicReader|sync\.Mutex' internal/eventid/eventid.go` returns at least one line.
- The package doc does not overclaim. Read `internal/eventid/eventid.go`'s package comment and confirm it states that this package makes minting monotonic and that commit ordering is enforced in the append transaction. A negative grep is deliberately not used here: an honest comment legitimately contains the word "monotonic" while scoping it, and a gate that fails on the correct sentence is worse than no gate.
- `TestEventID_StrictlyIncreasing` performs at least 1000 consecutive calls with no sleep between them — confirm by reading the loop bound.
- `task test ./internal/server/ ./internal/storage/...` is green with no pre-existing test modified.
- `task lint` exits 0 and `git diff` adds no line matching `nolint`.
  </acceptance_criteria>
  <done>One shared generator mints every event ID in non-test code, it never returns a value at or below one it has already returned or been seeded with, and a compaction provably advances the log maximum instead of pinning or regressing it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read the log high-water mark as the change ID</name>
  <files>internal/storage/storage.go, internal/storage/sqlite/eventstore.go, internal/storage/storagetest/suite.go</files>
  <read_first>
- internal/storage/storage.go (lines 51-68 — the `EventStore` interface this task extends; note `ListAggregateIDs`'s doc-comment style, and note that `AppendEvent`/`AppendEvents`/`AppendEventsBatch` all take caller-constructed envelopes, which is review H4 and what Task 3 closes)
- internal/storage/sqlite/eventstore.go (lines 194-206 — `ListAggregateIDs`, the `withConn` plus helper-function shape a new read method must follow)
- internal/storage/sqlite/projection.go (lines 310-340 — `getDistinctAggregateIDs`, the `sqlitex` query-helper convention for a plain SELECT)
- internal/storage/storagetest/suite.go (lines 655-700 — `RunAll` and the `EventStore*` subtest registration block; `internal/storage/sqlite/compliance_test.go:19` calls `RunAll`, so a new case there needs no edit to the sqlite test file)
- internal/storage/sqlite/migrations/001_initial.sql (lines 1-13 — `event_id TEXT PRIMARY KEY`, which makes `MAX()` an indexed seek rather than a scan)
- internal/eventid/eventid.go (Task 1's generator and its floor)
  </read_first>
  <behavior>
- `LatestEventID` on an empty event log returns the zero ULID and a nil error, never an error and never an empty string.
- `LatestEventID` after appending events returns the greatest appended `EventID`.
- Appending a further event advances the value `LatestEventID` returns.
- Compacting an aggregate advances the value `LatestEventID` returns (the storage-level mirror of Task 1's regression test, enforced against every backend).
- Every `EventStore` backend satisfies all of the above, because the case is registered in the shared compliance suite.
  </behavior>
  <action>
1. `internal/storage/storage.go` — add
`LatestEventID(ctx context.Context) (ulid.ULID, error)` to the `EventStore`
interface, placed after `ListAggregateIDs` and documented in that interface's existing
style: it returns the greatest `event_id` in the log, and returns the zero ULID with a
nil error when the log is empty.

State in the doc comment that this is the server's change ID (TMPL-08, D-18) and that
it is a high-water mark **because an implementation must not commit an event whose ID
sorts at or below the log's current maximum** — the ordering property is a requirement
on the *append* path, not a property of minting. Point at `internal/eventid` for the
generator and at the `AppendEvent` doc comment for the ordering obligation. Name the
consequence if the obligation is violated: the change ID would fail to advance across
a real state change and a deduping consumer would silently keep serving a stale zone.
Do **not** write that every append path mints its ID through one generator — that is
not true of an interface whose methods accept caller-constructed envelopes, and it is
the specific overclaim round-3 review H3/H4 rejected.

Also declare `ZeroChangeID` in this package as the canonical empty-store sentinel
string, so the sqlite implementation, the compliance suite and the server all agree on
one spelling (D-22). Document that it is the string form of the zero ULID and that a
consumer comparing change IDs must treat it as a value like any other — comparison is
exact string equality, never a prefix or numeric comparison.

2. `internal/storage/sqlite/eventstore.go` — implement
`func (s *Storage) LatestEventID(ctx context.Context) (ulid.ULID, error)` in the
`ListAggregateIDs` shape at `:194-206`: wrap the read in `s.withConn`, delegate to a
small unexported helper named `selectLatestEventID` that takes a `*sqlite.Conn` (so
Task 3 can reuse it from inside an open transaction), run
`SELECT MAX(event_id) FROM events`, treat a NULL result as the zero ULID, parse a
non-NULL result with `ulid.Parse`, and wrap failures with
`oops.Wrapf(err, "latest event id")`. Note in a comment that `event_id` is
`TEXT PRIMARY KEY` (`migrations/001_initial.sql:3`), so `MAX()` is an indexed seek
rather than a table scan. Do not add an index and do not add a migration.

3. `internal/storage/storagetest/suite.go` — add an `EventStoreLatestEventID` case and
register it in the `EventStore*` block of `RunAll` so every backend must satisfy it.
Cover: empty store returns the zero ULID and no error; after appending events the
returned ID equals the greatest appended `EventID`; and appending a further event
advances it. Add a second case `EventStoreCompactionAdvancesLatestEventID` in the same
block, asserting that a compaction leaves the value strictly greater than it was
before — this is the same property Task 1 pins in the sqlite package, lifted to the
level every backend must satisfy, so a future backend cannot reintroduce the
compaction bug in its own compaction path. `internal/storage/sqlite/compliance_test.go`
already calls `RunAll` at `:19`, so it needs no edit.

Commit as `feat(storage): derive change ID from newest event`.
  </action>
  <verify>
<automated>task test -- -run 'EventStoreLatestEventID|EventStoreCompactionAdvances' ./internal/storage/sqlite/ &amp;&amp; task test ./internal/storage/... &amp;&amp; task lint</automated>
  </verify>
  <acceptance_criteria>

- `task test -- -run 'EventStoreLatestEventID' -v ./internal/storage/sqlite/` reports at least 1 PASS line and 0 FAIL.
- `task test -- -run 'EventStoreCompactionAdvances' -v ./internal/storage/sqlite/` reports at least 1 PASS line and 0 FAIL.
- `git diff internal/storage/sqlite/compliance_test.go` is empty — both cases reach sqlite purely through `RunAll`.
- `rg -c 'LatestEventID' internal/storage/storage.go internal/storage/sqlite/eventstore.go internal/storage/storagetest/suite.go` returns at least 1 for each file.
- `rg -n 'ZeroChangeID' internal/storage/storage.go` returns at least 2 lines (declaration and doc comment).
- The interface doc comment locates the ordering obligation on the append path, not on minting: `rg -c 'eventid' internal/storage/storage.go` returns >= 1, AND `rg -ic 'append' internal/storage/storage.go` returns >= 1 in the `LatestEventID` comment. Then read the comment and confirm it does NOT assert that every append path mints its ID through one generator (review H3/H4) — a source assertion, checked by reading, because an honest comment legitimately uses the words "mint" and "generator" while scoping the claim.
- `selectLatestEventID` takes a connection rather than doing its own `withConn`, so Task 3 can call it inside an open transaction: `rg -n 'func selectLatestEventID' internal/storage/sqlite/eventstore.go` returns a line whose first parameter type is `*sqlite.Conn`.
- `rg -n 'MAX\(event_id\)' internal/storage/sqlite/eventstore.go` returns exactly one line — one derivation query, not two spellings.
- `git diff internal/storage/sqlite/migrations/` is empty — no schema change.
- `task test ./internal/storage/...` is green.
- `task lint` exits 0 and `git diff` adds no line matching `nolint`.
  </acceptance_criteria>
  <done>Any caller can ask the store for the ULID of its newest event, or the zero-ULID sentinel on an empty log, and every backend is held to that contract by the shared compliance suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: In-transaction ordering guard and startup floor seeding</name>
  <files>internal/storage/sqlite/eventstore.go, internal/storage/sqlite/sqlite.go, internal/storage/sqlite/eventid_guard_test.go, internal/storage/sqlite/sqlite_test.go, internal/storage/storage.go, internal/storage/storagetest/suite.go</files>
  <read_first>
- internal/storage/sqlite/eventstore.go (lines 313-335 — `insertEvent`, the single INSERT helper; and lines 21-52, 55-91, 93-130, 210-283 — `AppendEvent`, `AppendEvents`, `AppendEventsBatch` and `CompactAggregate`, to confirm with your own eyes that all four call `insertEvent` inside a `sqlitex.ImmediateTransaction` and that none of them uses the envelope's `EventID` after the insert returns)
- internal/storage/sqlite/sqlite.go (lines 69-115 — `Initialize`, its `withConn` body and where it ends; the seeding call goes after that body, not inside it)
- internal/storage/storage.go (Task 2's `LatestEventID` doc comment and the `AppendEvent`/`AppendEvents`/`AppendEventsBatch` doc comments this task amends)
- internal/storage/storagetest/suite.go (lines 20-32 — `makeEnvelope`, which mints with bare `ulid.Make()` at :27; line 72 — `require.Equal(t, env.EventID, got.EventID)`, the read-back equality this task's contract change affects; line 294 — a second bare mint)
- internal/storage/sqlite/sqlite_test.go (lines 45-90 — the local envelope helper at :53 and the read-back equality at :85, the same pattern as the compliance suite)
- internal/eventid/eventid.go (Task 1's `Seed` and `NewAfter` — the two entry points this task uses)
- .planning/phases/01-consumer-rendered-output-templates-sink/01-REVIEWS.md (round-3 H2, H3, H4 and M7 — the failure trace and the four deterministic cases this task must cover)
  </read_first>
  <behavior>
- Appending an envelope whose `EventID` sorts strictly above the log maximum stores that exact ID, unchanged.
- Appending an envelope whose `EventID` sorts at or below the log maximum still succeeds, and afterwards `LatestEventID` is strictly greater than it was before the append.
- Appending a batch of several envelopes in one transaction leaves `LatestEventID` strictly greater than before, even when every envelope in the batch carries an ID below the pre-existing maximum.
- Two goroutines that mint IDs in one order and commit in the opposite order both leave `LatestEventID` strictly greater than the value it held before their commit, so neither mutation is invisible to a consumer holding the other's ID.
- Opening a second `Storage` over the same database file and immediately appending yields an ID strictly greater than the persisted maximum, even when both land inside the same millisecond.
- Compacting an aggregate still advances `LatestEventID` (unchanged from Task 1, re-asserted through the guard).
  </behavior>
  <action>
This is the load-bearing half of the round-3 fix. Task 1 made minting monotonic within
a process; this task makes **commit** ordering authoritative, which is what makes mint
order irrelevant.

1. `internal/storage/sqlite/eventstore.go` — add the guard to `insertEvent` at `:313`,
before the `INSERT`. Read the current maximum with Task 2's `selectLatestEventID(conn)`.
When the log is non-empty and the envelope's `EventID` does not compare strictly
greater than that maximum, replace the local copy of the ID with
`eventid.NewAfter(max)` and insert that instead. `insertEvent` already receives its
envelope by value, so the replacement is local and no caller observes a mutated
struct.

**Re-mint rather than reject, and record why at the call site.** Rejecting would turn
a benign concurrent race into a user-visible write failure on a perfectly legitimate
`AddHost`, and there is no retry loop above `insertEvent` that would absorb it.
Re-minting keeps the write successful while making the log maximum advance, which is
the property the change ID actually needs. State in that comment that the ID a caller
proposes is therefore advisory, and that the invariant being enforced is "no commit
lands an ID at or below the log maximum" — **not** "every ID is minted monotonically",
which is not achievable for an interface that accepts caller-constructed envelopes.

Note in the same comment that the read is safe inside the transaction: every caller of
`insertEvent` holds a `sqlitex.ImmediateTransaction`, which takes SQLite's write lock
up front, so no other writer can commit between this read and this insert; and within
one transaction the `MAX` read sees this transaction's own earlier inserts, which is
what makes a multi-event batch correct without any extra bookkeeping. Note also that
`event_id` is `TEXT PRIMARY KEY`, so the extra read per insert is an indexed seek, and
that ULID strings are fixed-length Crockford base-32, so lexical string comparison is
the same ordering as `ulid.ULID.Compare`.

2. `internal/storage/sqlite/sqlite.go` — at the end of `Initialize`, **after** the
`withConn` body returns successfully, read `LatestEventID(ctx)` and pass it to
`eventid.Seed(...)`. Comment that this is what makes the process generator monotonic
against the **persisted** log rather than only against itself: without it, the first
ID minted after a restart that lands inside the same millisecond as the last
pre-restart append can sort below it (review H3). Do not fail `Initialize` on a
`LatestEventID` error that is merely an empty log — the zero ULID seeds harmlessly;
do propagate a real read error with `oops.Wrapf`.

3. `internal/storage/storage.go` — amend the doc comments on `AppendEvent`,
`AppendEvents` and `AppendEventsBatch` to state the obligation and its consequence for
callers: an implementation MUST NOT commit an event whose `EventID` sorts at or below
the store's current maximum, and it MAY therefore replace a proposed `EventID` that
does not. Callers must not assume the ID they supplied is the ID persisted. Point at
`LatestEventID` for why (the change ID is `MAX(event_id)`).

4. Route the envelope helpers that assert read-back ID equality through the shared
generator, so the guard never has cause to re-mint underneath them:
`internal/storage/storagetest/suite.go` `makeEnvelope` at `:27` and the loop mint at
`:294`, and `internal/storage/sqlite/sqlite_test.go` at `:53`. Change each
`ulid.Make()` to `eventid.New()`. `suite.go:72` and `sqlite_test.go:85` assert
`env.EventID == got.EventID`; that equality holds for any ID minted through
`eventid.New()` because the floor guarantees it sorts above anything the store already
holds, and it would otherwise flake whenever a bare `ulid.Make()` happened to land
below the maximum in the same millisecond. Leave `internal/server/metrics_test.go:627`
and `internal/server/commands_test.go:529,537` alone unless the compiler or a failing
assertion says otherwise — check, do not assume.

5. Add `EventStoreAppendNeverLowersLatestEventID` to
`internal/storage/storagetest/suite.go` and register it in the `EventStore*` block of
`RunAll`. Append events normally, record `LatestEventID`, then append an envelope whose
`EventID` is explicitly constructed to sort **below** that value, and assert the
outcome is one of exactly two acceptable ones: either the append returned an error and
`LatestEventID` is unchanged, or the append succeeded and `LatestEventID` is strictly
greater than the recorded value. Document in the case why the disjunction is the right
contract — sqlite re-mints, and a future backend may legitimately choose to reject, but
neither may leave a committed event below the maximum.

6. `internal/storage/sqlite/eventid_guard_test.go` (new) — the deterministic tests
review M7 asked for, none of which is probabilistic:

- `TestInsertEvent_LowerCallerSuppliedIDStillAdvancesMax` — append normally, capture
  `LatestEventID`, then `AppendEvent` an envelope whose `EventID` is a ULID
  constructed to sort below it (build one from a timestamp an hour in the past), and
  assert `LatestEventID` afterwards is strictly greater than the captured value.
- `TestAppendEventsBatch_AllLowerIDsStillAdvanceMax` — the same, with a batch of three
  envelopes all carrying past-timestamp IDs, asserting the maximum advanced once and
  the row count grew by three.
- `TestInsertEvent_ConcurrentReverseCommitOrderAdvancesMax` — mint ID_A, mint ID_B
  with `ID_B > ID_A`, commit B first and A second through two separate `AppendEvent`
  calls (sequential calls with deliberately inverted IDs reproduce the race
  deterministically without needing goroutine scheduling luck), and assert
  `LatestEventID` is strictly greater after A's commit than it was after B's. This is
  the direct regression test for review H2 — verify it goes RED with the guard removed
  before accepting it, and record that observation in the SUMMARY.
- `TestInitialize_SeedsGeneratorFromPersistedLog` — open a store over a temp file path,
  append an event, close it, open a **second** `Storage` over the same path, call
  `Initialize`, and immediately append an event minted by `eventid.New()`; assert its
  ID sorts strictly above the pre-existing maximum. To make the same-millisecond case
  deterministic rather than hoping the clock cooperates, seed the first store's event
  with an ID whose timestamp is in the **future** (an hour ahead): a fresh generator
  would otherwise certainly mint below it, so the test is RED without the seeding call
  and GREEN with it. Verify that RED before accepting.

Use `t.TempDir()` for every database path — never the real filesystem outside it.

Commit as `fix(storage): reject non-advancing event IDs in-transaction`.
  </action>
  <verify>
<automated>task test -- -run 'TestInsertEvent_|TestAppendEventsBatch_AllLower|TestInitialize_Seeds' ./internal/storage/sqlite/ &amp;&amp; task test -- -run 'EventStoreAppendNeverLowers' ./internal/storage/sqlite/ &amp;&amp; task test ./internal/storage/... ./internal/server/ &amp;&amp; task lint</automated>
  </verify>
  <acceptance_criteria>

- `task test -- -run 'TestInsertEvent_|TestAppendEventsBatch_AllLower|TestInitialize_Seeds' -v ./internal/storage/sqlite/` reports 4 PASS lines and 0 FAIL.
- `task test -- -run 'EventStoreAppendNeverLowers' -v ./internal/storage/sqlite/` reports at least 1 PASS line and 0 FAIL.
- The guard is in the shared INSERT helper, not duplicated per append path: `rg -n 'selectLatestEventID' internal/storage/sqlite/eventstore.go` shows a call inside `insertEvent` (read the function body to confirm), and `rg -c 'eventid\.NewAfter' internal/storage/sqlite/eventstore.go` returns 1.
- Every append path is covered by that one guard: `rg -n 'insertEvent\(' internal/storage/sqlite/eventstore.go` returns call sites inside `AppendEvent`, `AppendEvents`, `AppendEventsBatch` and `CompactAggregate` — enumerate all four by reading, since this is the H4 claim.
- The startup seeding exists and runs after migrations: `rg -n 'eventid\.Seed' internal/storage/sqlite/sqlite.go` returns exactly 1 line, and its line number is greater than the line number of the `withConn` call that opens `Initialize`'s migration body (confirm with `rg -n 'func \(s \*Storage\) Initialize|withConn|eventid\.Seed' internal/storage/sqlite/sqlite.go`).
- `TestInitialize_SeedsGeneratorFromPersistedLog` was observed RED with the `eventid.Seed` call removed, and `TestInsertEvent_ConcurrentReverseCommitOrderAdvancesMax` was observed RED with the guard removed. Both observations are recorded in the SUMMARY. A guard test never seen failing is indistinguishable from one that cannot fail.
- The interface contract records that a proposed ID may be replaced: `rg -ic 'replace' internal/storage/storage.go` returns >= 1 in the append doc comments, AND `rg -c 'LatestEventID' internal/storage/storage.go` returns >= 2. Read the comments and confirm they state the MUST NOT (no commit at or below the maximum) rather than a restatement of minting discipline.
- The read-back equality helpers mint through the generator: `rg -n 'ulid\.Make\(\)' internal/storage/storagetest/suite.go internal/storage/sqlite/sqlite_test.go` returns no matches, and `rg -c 'eventid\.New\(\)' internal/storage/storagetest/suite.go` returns at least 2. <!-- planner-discipline-allow: ulid.Make() --> Explain the substitution in the compliance case's own doc comment in general terms ("minted through the shared generator so the store never has cause to replace it"); do not name the replaced constructor in a comment in either file, or this gate fails on the comment.
- No schema change: `git diff internal/storage/sqlite/migrations/` is empty, and `rg -n 'ALTER TABLE|CREATE TABLE|AUTOINCREMENT' internal/storage/sqlite/eventstore.go internal/storage/sqlite/sqlite.go` shows no line added by this task (check `git diff`).
- No test writes outside a temp directory: `rg -n 'os\.MkdirTemp|t\.TempDir' internal/storage/sqlite/eventid_guard_test.go` returns at least 1 `t.TempDir` and `rg -n '"/tmp|"/var' internal/storage/sqlite/eventid_guard_test.go` returns no matches.
- `task test ./internal/storage/... ./internal/server/` is green with no pre-existing assertion weakened — `git diff` shows the only test-file edits are the three `ulid.Make()` to `eventid.New()` substitutions plus new files.
- `task lint` exits 0 and `git diff` adds no line matching `nolint`.
  </acceptance_criteria>
  <done>No event can be committed to the log with an ID at or below the log's maximum, from any append path, from any caller, in any mint order, and across a process restart — so `MAX(event_id)` advances across every real state change and the change ID cannot silently fail to move.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event append path to the change-ID derivation | Every write, from any caller, contributes the value the change ID is read from |
| compaction (storage layer) to the event log's ordering invariant | Compaction deletes and reinserts events without passing through the command layer |
| `EventStore` interface to arbitrary callers | The append methods accept caller-constructed envelopes, so the ID they carry is untrusted input to the ordering invariant |
| process lifetime to persisted log | A restarted process starts with a generator that knows nothing about rows already on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-33 | Tampering | a change ID that fails to advance across a real state change, letting a deduping consumer skip an update and serve a stale zone silently | high | mitigate | `MAX(event_id)` is sound only if no commit lands at or below the current maximum (D-20). Enforced in the append transaction at `insertEvent`, the single INSERT helper every append path shares, and asserted by `TestInsertEvent_LowerCallerSuppliedIDStillAdvancesMax`, `TestAppendEventsBatch_AllLowerIDsStillAdvanceMax` and the `EventStoreAppendNeverLowersLatestEventID` compliance case. The `internal/eventid` floor is the complementary mechanism, not the guarantee. |
| T-1-38 | Tampering | compaction inserting an event ID that sorts below the maximum it just deleted, moving the change ID backward | high | mitigate | Verified against `internal/storage/sqlite/eventstore.go:258`. Closed twice over: the compaction seed is minted through the shared generator, and the same insert passes through the in-transaction guard like every other append. Enforced by a sqlite regression test verified RED under the old code (with the observed failure count out of 50 recorded in the SUMMARY, review L3) and by an `EventStoreCompactionAdvancesLatestEventID` compliance case every backend must satisfy. |
| T-1-51 | Tampering | mint order not equal to commit order — a goroutine that minted an earlier ID committing after one that minted a later ID, leaving `MAX(event_id)` unmoved across a real mutation | high | mitigate | Added after review round-3 H2, verified against `internal/server/commands.go:115,119` running before `submitWrite` at `:135` (and the same shape at `:201-268`/`:284` and `:345`/`:354`). No process-local generator can close this, because the reorder happens after minting. The guard re-reads `MAX(event_id)` inside the write transaction and re-mints above it, so the committed ID is ordered by commit rather than by mint. Asserted deterministically by `TestInsertEvent_ConcurrentReverseCommitOrderAdvancesMax`, verified RED with the guard removed. |
| T-1-52 | Tampering | a process restart minting an ID inside the same millisecond as the last pre-restart append, sorting below it | medium | mitigate | Added after review round-3 H3. `Initialize` seeds `internal/eventid`'s floor from `LatestEventID` after migrations, so the first post-restart mint sorts strictly above the persisted maximum. Asserted by `TestInitialize_SeedsGeneratorFromPersistedLog`, made deterministic by seeding the pre-existing event with a future-timestamped ID so a fresh unseeded generator certainly mints below it; verified RED with the seeding call removed. The in-transaction guard is the backstop even if seeding is ever removed. |
| T-1-53 | Tampering | a caller constructing an `EventEnvelope` with an arbitrary `EventID` and appending it directly through the `EventStore` interface, bypassing every minting discipline | high | mitigate | Added after review round-3 H4, which noted `internal/storage/storagetest/suite.go:21` already does exactly this with bare `ulid.Make()`. The guard sits at the storage boundary rather than at the mint sites, so a caller-constructed ID is checked and re-minted on the way in. The interface doc comment states the obligation as a MUST NOT on implementations and warns callers that a proposed ID may be replaced; the compliance case holds every backend to it. Residual, accepted and named: the guard is implemented per backend, so a future `EventStore` that does not satisfy the compliance case reintroduces the hazard inside its own append path. |
| T-1-39 | Denial of Service | concurrent use of a non-goroutine-safe entropy reader or floor corrupting state or panicking under load | medium | mitigate | `*ulid.MonotonicEntropy` is not safe for concurrent use, and the generator is now reachable from the command path, from storage-layer compaction, and from inside the append transaction. The reader is wrapped in the library's `LockedMonotonicReader` where available, and the floor read-compare-write is held under a package `sync.Mutex` regardless. Asserted by a source grep and exercised by `TestEventID_ConcurrentUse` under the race detector. This is a data-race mitigation and is deliberately not the ordering mitigation — conflating the two is what made the round-2 plan's claim look stronger than it was. |
| T-1-40 | Information Disclosure | event IDs leaking write timing to a consumer | low | accept | A ULID's first 48 bits are the mint millisecond, so a change ID discloses when the newest event was written. This is unchanged from the pre-existing `EventID` design, is visible only to CA-verified mTLS consumers who can already read the full host inventory, and is the property that makes the ID sortable at all. The `next()` carry can push an ID's embedded millisecond marginally into the future, which narrows rather than widens this disclosure. |
| T-1-54 | Denial of Service | one extra `SELECT MAX(event_id)` per inserted row slowing the write path | low | accept | `event_id` is `TEXT PRIMARY KEY` (`migrations/001_initial.sql:3`), so the read is an indexed seek against an already-open connection inside a transaction that is already held. Writes in this system are operator-driven host mutations and periodic imports, not a high-throughput ingest path, and the alternative — a silently non-advancing change ID — is the failure TMPL-08 exists to prevent. |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable: `github.com/oklog/ulid/v2` is already a pinned direct dependency (`v2.1.2`) and every other import is stdlib. 01-RESEARCH.md's Package Legitimacy Audit records zero new external packages, and no package-manager install task exists in this plan. |

</threat_model>

<verification>
- `task test ./internal/eventid/ ./internal/server/ ./internal/storage/...` green under `-race`.
- `go.mod` and `go.sum` unchanged.
- `git diff internal/storage/sqlite/migrations/` empty — no schema migration, so D-18's wire form is untouched.
- Both compaction regression tests were observed RED against the pre-fix `ulid.Make()` seed, with the observed failure count out of 50 recorded.
- `TestInsertEvent_ConcurrentReverseCommitOrderAdvancesMax` was observed RED with the in-transaction guard removed.
- `TestInitialize_SeedsGeneratorFromPersistedLog` was observed RED with the startup seeding removed.
- `task lint` clean.
</verification>

<success_criteria>

- Exactly one non-test function in the tree mints an event ID, and it is monotonic against both its own history and the persisted log.
- No commit can land an event ID at or below the log maximum, from any append path, any caller, or any mint order — enforced inside the append transaction, not asserted in a comment.
- Compaction advances `MAX(event_id)` instead of pinning or regressing it, proven by tests that fail against the pre-fix code.
- `LatestEventID` returns the log high-water mark, or the zero ULID on an empty log, and every backend is held to that by the compliance suite.
- The change-ID premise TMPL-08 rests on is true in the code before any plan derives a change ID from it.
- Every claim in the plan text matches what the mechanism delivers: minting is monotonic per process, ordering is enforced at commit, and the per-backend residual is named rather than claimed away.
- D-18 and D-19 are unchanged: the change ID is still `MAX(event_id)`, still a ULID string on the wire, with no schema migration.
</success_criteria>

<output>
Create `.planning/phases/01-consumer-rendered-output-templates-sink/01-09-SUMMARY.md` when done
</output>
