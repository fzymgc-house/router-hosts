---
phase: 02-cursor-based-lazy-storage-reads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/service_export_golden_test.go
  - internal/server/hostsfile_golden_test.go
  - internal/client/template/template_golden_test.go
  - internal/storage/sqlite/projection_order_test.go
autonomous: true
requirements: [LAZY-04]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A byte-exact golden fixture exists for ExportHosts' hosts, json, and csv formats, captured against the CURRENT (pre-change) code and passing green before any behavior change lands (D-14)"
    - "With an empty inventory the json format emits exactly the two-byte literal [] (never null), the csv format emits the header row only, and sendExportChunks still sends exactly one response message carrying an empty chunk"
    - "A byte-exact golden fixture exists for the three regeneration writers (hosts file, unbound conf, dnsmasq conf), so D-10's ordering change cannot silently alter their output via D-07's ListAll wrapper"
    - "A byte-exact golden fixture exists for client-side consumer-template rendering (internal/client/template)"
    - "Today's de-facto aggregate-ID iteration order is pinned by an executed test against the real zombiezen driver, not inferred from reading the SQL (Q-01, memory b9gbp3bp20)"
    - "In the hosts format, two entries whose IP and hostname sort keys compare equal render in the same relative order as today — the sort's tie behavior is pinned, not merely its key order"
    - "Rendered entry order in json and csv output is pinned byte-for-byte so a later explicit ORDER BY aggregate_id can be proven a no-op rather than assumed one"
    - statement: "The goldens characterize single-goroutine rendering only; byte-identity of a render racing a concurrent write is not pinned by any fixture in this plan"
      verification: backstop
  prohibitions:
    - requirement_id: LAZY-04
      category: transparency
      status: unresolved
      statement: "MUST NOT regenerate, refresh, or re-record any golden fixture from post-change output. A golden captured after the behavior it is supposed to pin has already changed proves nothing and converts this phase's central byte-identity proof into a tautology. If a golden goes red, the change is wrong until proven otherwise."
  artifacts:
    - path: "internal/server/service_export_golden_test.go"
      provides: "Byte-exact ExportHosts hosts/json/csv goldens including the empty-inventory case"
      contains: "TestExportHosts_Golden"
    - path: "internal/server/hostsfile_golden_test.go"
      provides: "Byte-exact hosts-file writer golden with the volatile Last updated line stripped"
      contains: "TestFormatHostsFile_Golden"
    - path: "internal/client/template/template_golden_test.go"
      provides: "Byte-exact consumer-template render golden"
      contains: "TestTemplate_Golden"
    - path: "internal/storage/sqlite/projection_order_test.go"
      provides: "Executed pin of today's de-facto aggregate-ID order against the zombiezen driver"
      contains: "TestGetDistinctAggregateIDs_OrderMatchesDeFacto"
  key_links:
    - from: "internal/server/service_export_golden_test.go"
      to: "internal/server/service.go"
      via: "drives the real ExportHosts RPC through sendExportChunks and asserts the reassembled chunk bytes"
    - from: "internal/storage/sqlite/projection_order_test.go"
      to: "internal/storage/sqlite/projection.go"
      via: "calls the real Storage against a randomized-insertion fixture and asserts the returned aggregate order"
---

<objective>
Capture every byte-identity baseline this phase must preserve, BEFORE any production
behavior changes. D-14: LAZY-04 is proven by golden fixtures captured before any
behavior change, in the phase's first plan, covering every rendered surface —
`ExportHosts`' `hosts`/`json`/`csv`, the client-side consumer-template rendering, and
the three regeneration writers that D-07's `ListAll` wrapper puts downstream of D-10's
new ordering even though they do not migrate.

This plan also settles the one empirical question that gates D-10 by execution rather
than inspection: does today's `SELECT DISTINCT aggregate_id FROM events` already return
ascending order through the real zombiezen driver? RESEARCH.md answered this against the
`sqlite3` CLI and Python's `sqlite3` module; Assumption A1 explicitly asks an executor to
pin it against the production driver.

Purpose: this plan changes NO production code. It is the measuring instrument every
later plan in the phase is measured against. Every subsequent plan must keep these
fixtures green without editing them.

Output: four new test files, all green against unmodified production code.

**Tracer note (TRACER_MODE):** the phase's leading `type="tracer"` slice lives in plan
02-02. This plan precedes it because D-14 makes pre-change capture a hard ordering
constraint — the tracer's own byte-identity `<verify>` has nothing to compare against
until these fixtures exist. No task here alters observable behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cursor-based-lazy-storage-reads/02-CONTEXT.md
@.planning/phases/02-cursor-based-lazy-storage-reads/02-RESEARCH.md
@.planning/phases/02-cursor-based-lazy-storage-reads/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture byte-exact ExportHosts goldens for hosts, json, and csv</name>
  <read_first>
    - `internal/server/service.go` lines 646-757 — `exportChunkSize`, `exportChunkSender`, `sendExportChunks`, and `ExportHosts`' three format branches. This is the code under characterization; read it before writing a single expected byte.
    - `internal/server/dnsmasqconf_test.go` `TestFormatConf_Golden` (lines 57-82) — the repo's golden-fixture idiom: build a fixed `[]domain.HostEntry` literal, render, strip volatile lines, `require.Equal(t, want, got)`.
    - `internal/server/unboundconf_test.go` `TestUnboundFormatConf_Golden` (lines 66-93) — the multi-entry variant showing how multi-line `want` strings are concatenated.
    - `internal/server/service_test.go` — how existing tests stand up `HostsServiceImpl` and drive streaming RPCs; reuse that harness rather than inventing one.
    - `.planning/phases/02-cursor-based-lazy-storage-reads/02-RESEARCH.md` § Pitfall 2 — the `json.MarshalIndent` nil-slice-vs-empty-slice divergence this golden must pin.
  </read_first>
  <files>internal/server/service_export_golden_test.go</files>
  <action>
    Create `internal/server/service_export_golden_test.go` with `TestExportHosts_Golden`,
    driving the real `ExportHosts` RPC (not a re-implementation of its render logic) and
    reassembling the streamed `ExportHostsResponse.Chunk` byte slices into one buffer per
    format.

    Fixture: a fixed, deterministic `[]domain.HostEntry` set seeded through the real
    command/storage path so entry IDs are real ULIDs. Cover at minimum: an entry with a
    non-nil `Comment`, an entry with a nil `Comment`, an entry with multiple `Tags` and
    multiple `Aliases`, and an entry with empty `Tags`/`Aliases`. ULIDs are non-deterministic
    across runs, so assert with the real IDs substituted into `want` at assert time (build
    `want` from the seeded IDs) rather than hardcoding a ULID literal — the property being
    pinned is layout and ordering, not the ID values.

    Assert three format sub-cases plus a fourth empty-inventory sub-case:
    - `hosts` — full rendered output with the volatile `# Last updated:` line stripped, using
      the same line-filter approach as `TestFormatConf_Golden`.
    - `json` — the exact `json.MarshalIndent(out, "", "  ")` layout: two-space indent, no
      trailing newline, `comment` key absent when the entry's `Comment` is nil (the field
      carries `json:"comment,omitempty"`).
    - `csv` — header row exactly `id,ip_address,hostname,comment,tags,aliases`, `;` as the
      tags/aliases joiner, empty string for a nil comment.
    - empty inventory — `json` renders exactly `[]` (two bytes, never the four-byte literal
      `null`), `csv` renders the header row only, and the stream carries exactly ONE
      `ExportHostsResponse` message whose `Chunk` is empty. That last assertion is the review-L14
      empty-payload carve-out in `sendExportChunks`; D-03: the later streaming rewrite must
      preserve it, so it must be pinned here first.

    Count the messages the fake sender receives for the empty case and assert the count is
    exactly 1 — an assertion on the concatenated bytes alone cannot distinguish one empty
    message from zero messages, which is the exact bug shape review L14 already cost this repo.

    Do not modify `service.go`. If a golden cannot be written without extracting a render
    helper, stop and surface that rather than refactoring production code in this plan.
  </action>
  <verify>
    <automated>go test ./internal/server/... -run TestExportHosts_Golden -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/server/... -run TestExportHosts_Golden -race -count=1` passes against production code with zero non-test files modified (`git status --porcelain internal/server/service.go` reports no change).
    - The empty-inventory sub-case asserts a received-message count of exactly 1, and that assertion fails if the count is 0 or 2.
    - The empty-inventory `json` sub-case asserts the exact two-byte body `[]`; substituting `null` for that expectation makes the test fail.
    - The `json` sub-case's expected bytes contain no `comment` key for the nil-comment entry, and the test fails if that key is added.
    - `task test` passes (no other package's tests regress).
  </acceptance_criteria>
  <done>Four sub-cases (hosts, json, csv, empty inventory) green against unmodified `service.go`, with the empty-payload single-message carve-out asserted by message count.</done>
</task>

<task type="auto">
  <name>Task 2: Capture goldens for the three regeneration writers and the client consumer template</name>
  <read_first>
    - `internal/server/hostsfile.go` lines 45-89 — `FormatHostsFile`, the ONLY global sort in the export/regeneration path (line 54), and note it sorts the caller's slice IN PLACE.
    - `internal/server/hostsfile_test.go` — existing coverage, including `TestFormatHostsFile_SortedByIPThenHostname` and `TestFormatHostsFile_Empty`; the new golden must add byte-exactness, not duplicate these.
    - `internal/server/dnsmasqconf_test.go` and `internal/server/unboundconf_test.go` — `TestFormatConf_Golden` / `TestUnboundFormatConf_Golden` already exist; confirm by running them, and add nothing for those two writers if they are already byte-exact.
    - `internal/client/template/template.go` — `Parse`, `Render`, `FromProto`, `FuncMap`, `DeclaredVersion`.
    - `internal/client/template/template_test.go` — `TestExampleTemplates_UnboundMatchesExpectedOutput` and `TestExampleTemplates_DnsmasqAndHostsRender`; establish what is already byte-exact before adding.
  </read_first>
  <files>internal/server/hostsfile_golden_test.go, internal/client/template/template_golden_test.go</files>
  <action>
    Two files.

    (a) `internal/server/hostsfile_golden_test.go` — `TestFormatHostsFile_Golden`. D-14: the
    three regeneration writers must be pinned because D-07: `ListAll` becomes a drain-loop
    wrapper over `ListPage`, which puts all seven non-migrating callers downstream of D-10's
    new ordering. `hostsfile.go` is the one writer among the three with no existing byte-exact
    golden. Build a multi-entry fixture whose entries are deliberately supplied in NON-sorted
    order, render via `FormatHostsFile`, strip the volatile `# Last updated:` line the same way
    `TestFormatConf_Golden` does, and assert the full remaining output against an exact `want`.
    Include two entries whose IP-then-hostname sort keys compare equal (identical IP AND
    identical hostname, differing only in aliases or comment) so the sort's tie behavior — not
    only its key ordering — is pinned. Also assert `FormatHostsFile` still mutates the caller's
    slice order in place, since that in-place sort is an observable property callers may depend
    on and the later work must not silently change it.

    Run `TestFormatConf_Golden` and `TestUnboundFormatConf_Golden` and record in the SUMMARY
    that both are already byte-exact; add new tests for those two writers only if a run shows
    they are not.

    (b) `internal/client/template/template_golden_test.go` — `TestTemplate_Golden`. D-14 covers
    the client-side v0.13.0 consumer-template rendering. Parse a fixed template source,
    construct `Data` via `FromProto` from a fixed `[]*hostsv1.HostEntry` plus a fixed
    `SnapshotComplete`, call `Render`, and assert the exact output bytes. Cover a comment
    containing a newline so the `sanitize` FuncMap binding's collapsing behavior is inside the
    golden, and cover the empty-entry-set case. If the template exposes a generated-at or
    similar volatile value, strip that line the same way the server goldens do rather than
    freezing a clock.

    Do not modify any non-test file.
  </action>
  <verify>
    <automated>go test ./internal/server/... ./internal/client/template/... -run 'TestFormatHostsFile_Golden|TestFormatConf_Golden|TestUnboundFormatConf_Golden|TestTemplate_Golden' -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The combined `-run` command above passes with zero non-test files modified.
    - `TestFormatHostsFile_Golden` supplies its fixture entries in non-sorted order and its `want` reflects sorted output — reversing the fixture's input order leaves the test green (proving the assertion is on rendered order, not input order).
    - `TestFormatHostsFile_Golden` includes at least two entries with identical IP and identical hostname, and asserts their rendered relative order.
    - `TestTemplate_Golden` includes a comment value containing a literal newline and asserts the exact rendered bytes for it.
    - The SUMMARY records the observed pass/fail of `TestFormatConf_Golden` and `TestUnboundFormatConf_Golden` as run output, not as an assumption.
    - `task test` passes.
  </acceptance_criteria>
  <done>`TestFormatHostsFile_Golden` and `TestTemplate_Golden` green; the two pre-existing conf goldens confirmed green by execution.</done>
</task>

<task type="auto">
  <name>Task 3: Pin today's de-facto aggregate-ID order against the real zombiezen driver</name>
  <read_first>
    - `internal/storage/sqlite/projection.go` lines 314-333 — `getDistinctAggregateIDs`, the bare `SELECT DISTINCT aggregate_id FROM events` with no `ORDER BY`.
    - `internal/storage/sqlite/migrations/001_initial.sql` — the `events` schema and `idx_events_aggregate(aggregate_id, event_version)` covering index.
    - `internal/storage/sqlite/sqlite_test.go` — the `StorageSuite` bootstrap (`New`, `Initialize`, temp path) this test should reuse rather than reinvent.
    - `.planning/phases/02-cursor-based-lazy-storage-reads/02-RESEARCH.md` § Open Questions Q-01 and § Assumptions Log A1 — the cross-implementation proof already performed, and the explicit instruction that an executor must re-prove it against the production driver.
  </read_first>
  <files>internal/storage/sqlite/projection_order_test.go</files>
  <action>
    Create `internal/storage/sqlite/projection_order_test.go` with
    `TestGetDistinctAggregateIDs_OrderMatchesDeFacto`.

    D-10: the keyset forces an explicit `ORDER BY aggregate_id` replacing today's bare
    `SELECT DISTINCT aggregate_id FROM events`. Before that lands, prove by execution — never
    by reading the query — that the current bare form already returns ascending ULID-TEXT order
    through `zombiezen.com/go/sqlite`.

    Seed a store with at least 200 aggregates whose IDs are generated and then appended in
    deliberately RANDOMIZED order (shuffle before appending), with a varying number of events
    per aggregate so the index scan is not trivially sequential. Then call the current
    `getDistinctAggregateIDs` (or `ListAll` and derive the order from the returned entries if
    the unexported helper is not reachable from the test's package position — it is in package
    `sqlite`, so direct access is available) and assert the returned slice is strictly
    ascending by `ulid.ULID.Compare`.

    Assert strict ascent (each element strictly greater than its predecessor), not merely
    sortedness-after-sorting, and assert the returned set equals the seeded set so a truncated
    result cannot pass.

    Add a second sub-case asserting the same property holds after an aggregate is deleted
    (tombstoned) and after `CompactAggregate` runs on one aggregate — RESEARCH.md Pitfall 5
    records that compaction reuses the same `aggregate_id` with a freshly minted `event_id`,
    so the aggregate's keyset position must not move. Pinning that now means a later
    regression in compaction cannot silently reorder the read path.

    Use `oops`-free plain test assertions via `testify/require`, matching the file's neighbors.
    Do not modify `projection.go`.
  </action>
  <verify>
    <automated>go test ./internal/storage/sqlite/... -run TestGetDistinctAggregateIDs_OrderMatchesDeFacto -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/storage/sqlite/... -run TestGetDistinctAggregateIDs_OrderMatchesDeFacto -race -count=1` passes with zero non-test files modified.
    - The fixture appends aggregates in shuffled order; the test still asserts ascending output, so it would fail if the query returned insertion order.
    - The test asserts set equality against the seeded ID set, so a result truncated to its first N elements fails.
    - The compaction sub-case asserts the compacted aggregate occupies the same index in the returned order as it did before compaction.
    - The SUMMARY records the observed result as executed output, satisfying RESEARCH.md Assumption A1's requirement that Q-01 be re-proven against the production driver.
  </acceptance_criteria>
  <done>Today's de-facto ascending aggregate order is a green, executed test against the real driver, including across a delete and a compaction.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by THIS PLAN (not pre-existing; exclude from drift verification):

| Kind | Name / Path |
|---|---|
| file | `internal/server/service_export_golden_test.go` |
| file | `internal/server/hostsfile_golden_test.go` |
| file | `internal/client/template/template_golden_test.go` |
| file | `internal/storage/sqlite/projection_order_test.go` |
| test func | `TestExportHosts_Golden` |
| test func | `TestFormatHostsFile_Golden` |
| test func | `TestTemplate_Golden` |
| test func | `TestGetDistinctAggregateIDs_OrderMatchesDeFacto` |

Created by LATER plans in this phase (also new, listed here so reviewers do not flag them
as drift): `ListPage` (method on `storage.HostProjection` and on `sqlite.Storage`),
`getAggregateIDPage`, `defaultListPageSize`, `TestHostProjectionListPage`,
`TestHostProjectionListPage_CompactionAhead`, `TestHostProjectionListPage_CompactionBehind`,
`internal/storage/sqlite/projection_bench_test.go`, `peakHeapDuring`, `BenchmarkPeakMemory`,
the `lazybench` build tag, the `test:bench:lazy` Taskfile target, and the `bench-lazy`
CI job in `.github/workflows/ci-go.yml`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → production render path | Test-authored `[]domain.HostEntry` values cross into `ExportHosts` / `FormatHostsFile` / the client template. No untrusted external input crosses any boundary in this plan; it adds test files only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Repudiation | golden fixtures (`service_export_golden_test.go`, `hostsfile_golden_test.go`, `template_golden_test.go`) | high | mitigate | A golden regenerated from post-change output silently destroys the phase's byte-identity evidence. Mitigated by the `must_haves.prohibitions` entry forbidding regeneration and by capturing every fixture in wave 1, before any behavior-changing plan runs. |
| T-02-02 | Tampering | `internal/client/template` render path | low | accept | Consumer templates are operator-authored local files, already covered by the v0.13.0 `sanitize` FuncMap and `RequireVersion` gate; this plan only characterizes existing behavior and adds no new input path. |
| T-02-SC | Tampering | package-manager installs | low | accept | No new external packages. RESEARCH.md § Package Legitimacy Audit records the gate as not applicable — every library used is already in `go.mod`. No install task exists in this plan, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `task test` green (full `-race` suite) with zero non-test files modified across the whole plan.
- All four new test functions pass by name.
- `task lint` green (`golangci-lint` + `buf lint`).
- `git status --porcelain` shows only the four new `_test.go` files as changes.
</verification>

<success_criteria>
- Every rendered surface named in D-14 has a byte-exact fixture captured against pre-change code: `ExportHosts` `hosts`/`json`/`csv` (incl. empty inventory), the hosts-file writer, the two conf writers (confirmed by execution), and the client consumer template.
- The empty-inventory single-message carve-out is pinned by message COUNT, not only by bytes.
- Today's de-facto ascending aggregate-ID order is proven by an executed test against `zombiezen.com/go/sqlite`, closing RESEARCH.md Assumption A1.
- No production file changed.
</success_criteria>

<output>
Create `.planning/phases/02-cursor-based-lazy-storage-reads/02-01-SUMMARY.md` when done.
Record verbatim run output for the Q-01 order test and for the two pre-existing conf
goldens — RESEARCH.md's proof used a different SQLite implementation, and this SUMMARY is
where the production-driver proof lives.
</output>
