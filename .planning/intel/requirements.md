# Requirements (PRD Intel)

No PRD-typed documents were present in the ingest set (0 of 27 classified as PRD). No requirements with formal acceptance criteria were extracted, and no competing acceptance variants exist.

Downstream note for `gsd-roadmapper`: requirements must be derived from the SPEC constraints (`constraints.md`) and the locked ADR decisions (`decisions.md`), since this codebase's intent is expressed through design specs and accepted ADRs rather than product requirement docs. Treat the SPEC scope items as feature-level requirement candidates, gated by the four locked ADR decisions.
